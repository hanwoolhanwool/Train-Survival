# M5 6차 플레이 검증 항목 — 파지·열차 하부 즉사 존·처형 제거 (+ 5차 이월)

작성일: 2026-08-09 · 대상: `feat/m5-combat-survival` · 상태: **1차 검증 통과 (2026-08-10) — 코어 성립,
버그 1번(투척 지연) 해결 확인. 잔여 = F(사막)·G(이월)·C3·C4·B19·버그 2번(기절체 컨베이어) 재확인**

> **6차 목표** (계획 문서): **"몬스터를 낚아채면 놓을 때까지 집게에 매달려 있고, 든 채로 이동해
> 아군 앞이나 열차 바퀴에 넘겨 처치하는" 루프가 종단으로 성립**한다 — 처형 배율이 아니라
> **운반**이 집게의 가치가 된다. 놓인 몬스터는 잠깐 기절했다가 복귀한다 (처형 창 아님).
> **검증 구성**: 개발 6원칙 1·2에 따라 **항상 호스트 1 + 클라이언트 1**로 확인한다.
> **5차 이월 20건을 이번 검증에 편입한다** (→ G 구역). 단 5차 E4~E8(그로기)·F1~F7(처형)의
> 통과 기록은 이번 설계 반전으로 **역사 기록**이 됐다 — A·B 구역이 새 명세로 대체한다.

---

## 0. 사전 준비

- [ ] Boot 씬에서 Play → 호스트 시작. 클라이언트 1대 접속(MPPM 가상 플레이어 또는 빌드)
- [ ] 이번 차수는 **프리팹·씬 변경이 없다** (컴포넌트 추가 없음 — 해시 리스크 없음).
      접속이 거부되면 다른 원인이다
- [ ] 콘솔을 열어둔다 — 그랩 **승인**(`[HarpoonController] 그랩 승인: …`, 6차 신규)·거부 로그와
      QA 키 결과(피해 실측·동시 그랩)가 로그로 나온다

**디버그 키** (숫자패드 0·− 신규)

| 키 | 동작 |
|---|---|
| 숫자패드 1 / 2 | 낮 / 밤 국면 점프 |
| 숫자패드 3 | 다음 Day 아침으로 — 사막 진입(Day 6+)에 반복 사용 |
| 숫자패드 9 | 자원 지급: 목재 4 · 돌 2 · 고철 2 · 화약 원료 2 · 식재료 4 |
| 숫자패드 6 | 부위 샘플 데미지 30 — 망치로 겨눈 부위에 |
| 숫자패드 5 | 몬스터 웨이브 스폰 토글 |
| 숫자패드 4 | 공유 창고 동시 경합 재현 |
| **숫자패드 0** | **피해 실측** (6차 신규 — 요청자에게 고정 피해 20, 물리 경로 = 장비 감산 적용) |
| **숫자패드 −** | **동시 그랩** (6차 신규 — 전 피어가 같은 프레임에 최근접 자원 노드에 그랩 요청) |
| 숫자패드 + | 세션 재시작 |

**기준 수치** (판정 근거 — 전부 데이터 에셋, 제안값이므로 플레이 검증에서 조정)

| 항목 | 에셋 | 값 |
|---|---|---|
| 파지 앵커 (전방 / 측면 / 높이) | `HarpoonSettings` `_holdDistance` / `_holdSide` / `_holdHeight` | 1.5 m / **−0.8 m (왼쪽 — 화면 좌측 앞)** / 1.2 m |
| 투척 (속도 / 사거리 / 피해 / 판정 반경) | `HarpoonSettings` `_throwSpeed` / `_throwRange` / `_throwDamage` / `_throwRadius` | 20 m/s / **8 m (짧게)** / **60** (돌진형 60 즉사 · 일반형 100 반피 이상) / 0.5 m |
| 놓였을 때 기절 | `MonsterSettings` 4종 `_stunDurationSeconds` | **2.5초** (5차 5초에서 하향 — 의미가 처형 창 → 이탈 틈으로 바뀜) |
| 처형 배율 | — | **필드 자체가 삭제됨** (`_stunnedDamageMultiplier` 제거) |
| 열차 하부 즉사 존 높이 | `TrainLayoutSettings` `_wheelKillHeight` | 1.2 m (**0 = 존 비활성**) |
| 스튜 즉시 체온 상한 (평시 / 더위 국면) | `TemperatureSettings` `_instantWarmthCeiling` / `_instantWarmthCeilingHot` | 38 ℃ / **39 ℃** (고온 피해 임계 직전) |

---

## A. 처형 부재 확인 (역방향 — 설계 반전의 확인)

5차 F 구역(처형 7건)·K10·K11의 통과 기록을 **무효화하는 새 명세**다. 처형 축이 정말 사라졌는가.

- [0] **A1** 파지·기절 중인 몬스터를 근접(마체테)으로 쳐도 **한 방에 죽지 않는다** — 일반 데미지만 들어간다
- [0] **A2** 파지·기절 중인 몬스터를 총으로 쏴도 동일하다 (배율 없음 — 체력만큼 때려야 죽는다)
- [0] **A3** 몬스터를 어떤 방식으로 죽여도 **"처형!" 배너가 뜨지 않는다** (표면 제거 확인)
- [0] **A4** 평상시 몬스터 전투가 5차와 동일하다 (배율 제거는 평시 데미지에 영향 없음)
- [0] **A5** 처치 수(HUD)는 여전히 정상적으로 오른다 (이벤트 시그니처 복귀의 회귀 확인)


## B. 파지 (5차 E 구역 대체 — 새 명세, 2026-08-10 "원하는 방향" 반영)

핵심 반전: **도착 = 소멸도 기절도 아닌 파지 유지**. 5차의 견인 파이프라인(E1~E3·E13~E15)은
그대로이고, 도착 이후가 바뀌었다.
**2차 반영 (2026-08-10)**: ① 파지 중 좌클릭 = **투척(슬램)** ② **무기를 바꿔도 놓지 않는다**
③ 앵커가 **화면 좌측 앞**이고 전 피어가 **로컬 부착**해 홀더와 **한 몸처럼** 움직인다.

- [0] **B1** 몬스터를 낚아채 릴이 끝나면 **소멸하지 않고 화면 좌측 앞에 매달린다** (전방 1.5 m · 왼쪽 0.8 m · 높이 1.2 m)
- [0] **B2** 파지 중 **로프·훅이 계속 보인다** (회수되지 않는다)
- [0] **B3** **든 채로 이동하면 몬스터가 한 몸처럼 붙어 움직인다** — 걷기·달리기·방향 전환에
      "뒤따라오는" 지연 없이 **화면의 같은 자리에 고정**된 느낌 (로컬 부착 — 스냅샷 보간을 덮어쓴다)
- [0] **B4** 파지 중 몬스터는 **공격·이동하지 않는다** (견인 정지 유지)
- [0] **B5** **파지 중 좌클릭 = 투척(슬램)**: 든 몬스터가 조준 방향으로 발사된다 — 사거리는 짧다 (8 m)
- [0] **B6** **놓기 ① 우클릭**: 몬스터가 그 자리에 떨어지고 **잠깐 기절**한다 (노란색 + 기운 자세, 2.5초)
- [0] **B7** **무기를 바꿔도 놓지 않는다** — 파지·로프가 유지되고, 든 채 다른 무기로 쏠 수 있다.
      놓기·투척은 **집게로 돌아와서** 한다 (우클릭·좌클릭)
- [0] **B8** **놓기 ② 대상 사망**: 파지 중 팀원(또는 무기를 바꾼 본인)이 쏴 죽이면 로프가 정리되고
      되감기로 복귀한다 (5차 E12의 새 명세)
- [0] **B9** 기절이 끝나면 **정상 복귀**하고 다시 추격한다 — 복귀 후 재그랩 가능
- [0] **B10** **기절 중에는 재그랩되지 않는다** (5차 E8 유지)
- [0] **B11** **견인 중(도착 전) 우클릭 취소는 기절 없이** 즉시 복귀한다 (5차 E13 유지 — 파지에서 놓인 경우에만 기절)
- [0] **B12** "놓고 무기 들어 싸우기"가 성립한다 — 놓기 → 기절 2.5초 안에 무기 교체 → 선제 타격
- [0] **B13** **게스트 홀더도 성립한다** — 게스트가 파지한 채 이동해도 호스트·게스트 화면 모두 매달려 따라온다
- [0] **B14** 게스트 화면에서 호스트의 파지(매달린 몬스터·로프)가 같게 보인다 — 원격 화면에서도
      몬스터가 **홀더에 붙어** 움직인다 (부착 기준이 각자 화면의 홀더 위치라 홀더와 어긋나지 않는다)
- [0] **B15** **자원 회수는 한 줄도 바뀌지 않았다** — 자원은 5차 그대로 도착 = 수납·소멸 (enum 분기 회귀 확인)

**투척(슬램) — 2차 반영 신규**

- [0] **B16** 투척한 몬스터가 **지형·구조물에 부딪히면 큰 피해(60)를 받고 그 자리에 떨어져 기절**한다
      — 돌진형(체력 60)은 즉사, 일반형(100)은 반피 이상 깎인 채 기절
- [0] **B17** 사거리(8 m) 끝까지 아무것도 안 맞으면 **그 자리에 떨어져 기절만** 한다 (피해 없음)
- [0] **B18** 투척 비행 모습이 게스트 화면에도 보인다 (견인 채널 재사용 — 비행 중에는 부착이 풀린다)
- [바퀴존이 어디인지 모르겠다. → 아래 확인법 참조, 재확인 대기] **B19** **바퀴 존으로 던져 넣어도 즉사**한다 (투척 경로가 즉사 존을 지나면 C 구역 규칙 적용)
- [0] **B20** 투척 후 집게는 쿨다운을 거쳐 정상 복귀한다 — 재발사·재그랩 가능

> **B19 확인법 — "바퀴 존"은 눈에 보이는 바퀴가 아니라 열차 칸 아래 공간 전체다**:
> 열차 발자국 안(**|x| ≤ 1.5 m** — 칸 폭 3 m의 절반, **z −19.5 ~ +19.5** — 편성 전체 길이)이면서
> **높이 y ≤ 1.2 m**인 영역. 표시가 따로 없으므로(연출은 이월) 위치로 판단한다.
> 던져 넣기: **지상에서 열차 옆 2~3 m 지점에 서서 열차 몸통 아래쪽을 조준하고 좌클릭** —
> 투척 경로가 열차 밑으로 들어가는 순간 즉사한다. C1(들이밀기)이 이미 통과했으므로
> 같은 영역에 던지는 것만 확인하면 된다.

> ⚠ 검증 중 발견 — **투척 좌클릭 시 클라이언트 반응 지연** (호스트가 먼저 작동한 뒤 클라이언트가
> 작동): → "발견된 버그" 1번. **수정 완료 (2026-08-10)** — 재확인 대상.
## C. 열차 하부 즉사 존

- [0] **C1** **파지한 채 지상에서 열차 옆으로 걸어가 바퀴 쪽에 들이밀면** 몬스터가 즉사한다 (주 동선)
- [0] **C2** 즉사 시 **처치 수가 오르고**, 파지가 풀리며 로프가 정리된다 (귀속 = 바퀴에 넣은 사람)
- [나중에 테스트.] **C3** **놓인 기절 몬스터가 존 안에 있으면** 즉사한다 (예: 열차 발자국 안 지면에 놓기)
- [테스트로 확인하기에는 상황이 애매하다. 나중에 확인해봄. ] **C4** 견인 중(끌려오는 중) 경로가 존을 지나도 즉사한다
- [0] **C5** **자유 몬스터는 죽지 않는다** — 밤 웨이브 몬스터가 추격 중 열차 밑을 스쳐도 무사 (밸런스 안전선)
- [0] **C6** **갑판 위에서 파지한 채 서 있으면 죽지 않는다** (갑판 높이 3 m > 존 높이 1.2 m)
- [0] **C7** 게스트가 바퀴에 넣어도 동일하다 (판정 = 호스트, 귀속 = 게스트)

## D. 제작 창 발견성 (5차 개선 1번)

- [0] **D1** 제작 창이 **화면 높이의 70 %를 넘지 않는다** — 목록이 길면 스크롤이 생긴다
- [0] **D2** **분류 머리글**이 보인다 — 순서: **집게 강화 → 탄약 → 무기·장비 → 요리**
- [0] **D3** **승급 줄이 최상단 그룹**이다 (5차에서 12번째 줄이라 못 찾던 문제의 해소)
- [0] **D4** 재료 부족 줄의 버튼 라벨이 **"재료 부족"** 이다 — 회색이어도 "없는 것"처럼 보이지 않는다
- [0] **D5** 화덕 근처에서만 요리 그룹이 나타난다 (기존 지점 필터와 그룹화의 결합 확인)
- [0] **D6** **제작 요청은 그대로 동작한다** — 그룹 순서가 바뀌어도 올바른 레시피가 제작된다 (인덱스 규약 무변)

## E. QA 수단 2종 (5차 개선 4·8번)

- [0] **E1** **숫자패드 0**: 호스트 콘솔에 `피해 실측: … 체력 N → N−20` 이 찍히고 HUD 체력이 20 준다 (맨몸)
- [0] **E2** 게스트가 눌러도 **게스트 본인**이 맞는다 (요청자 귀속)
- [0] **E3** **숫자패드 −**: 콘솔에 `동시 그랩 트리거: 대상=…` 후 **승인 1건 + 거부 1건**이 찍힌다
- [패스] **E4** 그랩 가능한 자원이 하나도 없으면 `동시 그랩 무효` 로그가 찍히고 아무 일도 없다 (전제 검사)

## F. 스튜 즉시 체온 상한의 국면화 (5차 G3)

- [ ] **F1** **사막 낮(45 ℃)에 스튜를 먹으면 39 ℃까지 오른다** (5차에는 38 ℃에서 잘렸다)
- [ ] **F2** 39 ℃는 **고온 경고(38 ℃) 구간이지만 피해(> 39 ℃) 구간은 아니다** — 음식으로 피해를 입지 않는다
- [ ] **F3** **평시(쾌적·추위 국면)에는 기존 38 ℃ 상한 그대로**다 (사막 밤 포함)
- [ ] **F4** 이미 상한 위일 때 먹어도 **끌어내려지지 않는다** (5차 G4 유지)

## G. 5차 이월 편입 (20건)

**QA 키로 풀린 항목** (E 구역과 연동)

- [0] **G1** (5차 H7 — 3·4·5차 연속) **가죽 옷 15 % 실측**: 맨몸 숫자패드 0 = **−20**, 착용 후 = **−17**
- [ ] **G2** (5차 I1) **동시 그랩 경합 (자원)**: 숫자패드 − → 한쪽 승인 + 한쪽 "다른 사람이 잡고 있다" HUD 안내
- [ ] **G3** (5차 I2) 몬스터 동시 그랩도 같은 규칙으로 갈린다 (2인 수동 — 같은 몬스터를 동시에 쏜다)

**언제든 가능**

- [ ] **G4** (5차 C3) 원목을 1단계 집게로 쏘면 콘솔에 `verdict=InsufficientTier` — **Q4 불일치에 세지 않는다**
- [ ] **G5** (5차 D1) 광맥 보유 시 제작 창에 **대량 탄약 3줄**이 보인다 (이제 "탄약" 그룹 안)
- [ ] **G6** (5차 D2·D3) 대량 제작 산출: 권총탄 12 / 산탄 6 / 소총탄 9
- [ ] **G7** (5차 D4) 넣을 자리가 없으면 전량 실패 + 재료 보존
- [ ] **G8** (5차 D5) 기본 탄약 레시피도 그대로 동작한다
- [ ] **G9** (5차 D6) 게스트의 대량 제작도 동일하게 확정된다
- [ ] **G10** (5차 I3) 두 플레이어가 **다른 등급**일 때 각자 사거리·속도로 동작한다

**Day 조건**

- [ ] **G11** (5차 E9) 1단계 집게로 돌격형(Day 6+)·도약형(Day 8+)을 쏘면 거부된다 (무게 2)
- [ ] **G12** (5차 E10) 2단계 이상이면 잡힌다 — 잡히면 이제 **파지**로 이어진다
- [ ] **G13** (5차 E11) **갑판 위 몬스터를 그랩해 뜯어내 들고 내려온다** — 파지 재설계로 의미가 커진 경로

**조건·시간 확보 시 (순번 뒤로)**

- [ ] **G14** (5차 H1) 화덕 + 제작대 인접 배치 시 양쪽 레시피가 다 보인다
- [ ] **G15** (5차 H3) 몬스터가 화덕을 표적으로 삼는다
- [ ] **G16** (5차 H6) 사망 중 수리·건설·재결합 차단 (부활 지연 20초로 올려 확인 후 원복)
- [ ] **G17** (5차 H9) 기아 피해가 장비 피해 감소를 받지 않는다 (400초 관측)
- [ ] **G18** (5차 H11 — 2~5차 연속) 낮 채집 → 밤 웨이브 → Day+1 실시간 전체 사이클

## H. 회귀 확인

- [0] **H1** 기존 채집 → 제작 → 사격 루프가 그대로 동작한다 (B15가 부분 커버)
- [0] **H2** 밤 웨이브 스폰·추격·열차 공격이 그대로 동작한다 (즉사 존 대상 제한 C5가 핵심)
- [0] **H3** 요리·허기·체온 루프가 그대로 동작한다 (F 구역 외 변경 없음)
- [0] **H4** 손잡이 앵커(이탈 칸 붙잡기)가 그대로 동작한다 (enum 전환 회귀 — Rejected 경로)
- [0] **H5** EditMode **302개 통과** — 2026-08-09 구현 완료 시점: **302/302 (3.6초)**
      (파지 전이 5건 · 즉사 존 4건 · 상한 국면화 3건 신규 포함)

## I. 데이터 조정 가능성 (에셋 수정만으로)

5차 K 구역과 같은 방식 — 코드 경로 검수 + 검산으로 정적 확인 (사용자 위임, 2026-08-10 수행).
각 수치가 **단일 에셋 프로퍼티에서만 소비**되고 코드에 중복 하드코딩이 없음을 grep으로 확인했다
→ "에셋 수정만으로 반영"이 구조적으로 성립한다. 에셋을 실제로 바꾸지 않았으므로 원복 절차 불필요.

- [x] **I1** 파지 앵커 1.5 → 3 m — **성립**: `HoldDistance`·`HoldSide`·`HoldHeight`의 유일 소비처가
      `ComputeHoldAnchor`(`HarpoonController.cs:518~520`) 하나이고, **서버 판정**(ServerUpdateTow)과
      **전 피어 표시**(MonsterGrabTarget.LateUpdate 로컬 부착)가 둘 다 이 메서드를 경유한다 —
      값을 바꿔도 화면과 판정이 갈리지 않는다
- [x] **I2** 기절 2.5 → 0초 — **성립**: `StunDurationSeconds`의 유일 소비처가
      `MonsterGrabTarget.ReleaseGrab`(`:305`)이고, `duration ≤ 0`이면 기절 진입 없이 반환한다 —
      놓으면 즉시 추격 재개 (축 끄기)
- [x] **I3** 즉사 존 1.2 → 0 — **성립**: `WheelKillHeight`의 유일 소비처가
      `ServerCheckWheelKillZone`(`:243`)이고, 순수 함수 `IsInWheelKillZone`은 `killHeight > 0` 선행
      조건이라 0이면 항상 거짓이다. EditMode `높이_0이면_존이_비활성이다`가 이 경계를 고정한다
- [x] **I4** 더위 상한 39 → 41 — **성립 + 검산 일치**: `InstantWarmthCeilingHot`의 유일 소비처가
      `PlayerTemperature.cs:88`(ResolveWarmthCeiling 인자)다. 41 ℃는 고온 피해 임계(39 ℃)를
      넘으므로 (41 − 39) × 3 = **초당 6 피해**가 실제로 들어온다 — 상한이 진짜 방지선임이 확인된다
      (5차 K13과 같은 구조)
- [x] **I5** 투척 피해 60 → 999 — **성립**: `ThrowDamage`의 유일 소비처가
      `ServerUpdateThrow`(`:559`)의 `ApplyDamage`이고 피해량에 상한이 없다 — 999면 돌격형(220)도
      슬램 한 방. 단 죽지 않는 경우의 기절은 놓임 경로 소유라 배율 축(처형)과 달리 조정이 국소적이다
- [x] **I6** 투척 사거리 8 → 20 — **성립**: `ThrowSpeed`·`ThrowRange`·`ThrowRadius`가 **서버 비행**
      (`:547~567`)과 **소유자 선반영**(`:218`, 투척 지연 수정으로 신설) 두 곳에서 같은 에셋 값으로
      소비된다 — 예측과 판정이 같은 수치로 움직여 값을 바꿔도 어긋나지 않는다
---

## 결과 요약

1차 검증 2026-08-10 · 호스트 1 + 클라이언트 1.

| 구분 | 1차 | 비고 |
|---|---|---|
| A. 처형 부재 확인 | **통과 (5/5)** | 처형 축이 정말 사라졌다 — 배율·배너 부재, 처치 수 정상 |
| B. 파지 | **통과 (19/20)** | 코어 루프 전부 성립. B19만 보류(바퀴 존 위치 안내 후 재확인). 결함 1건 발견 → 수정 완료 (버그 1번) |
| C. 열차 하부 즉사 존 | **통과 (5/7)** | 주 동선(C1)·귀속(C2·C7)·안전선(C5·C6) 성립. C3·C4는 상황 조성이 애매해 후속 |
| D. 제작 창 발견성 | **통과 (6/6)** | 5차 1차 검증을 막았던 발견성 문제 해소 확인 |
| E. QA 수단 2종 | **통과 (3/4)** | E4(자원 전무 전제 검사)는 상황 조성이 번거로워 패스 |
| F. 스튜 상한 국면화 | 미검 (0/4) | 사막 진입(Day 6+) 필요 — 후속 |
| G. 5차 이월 (20건) | 부분 (1/20) | **G1 통과 — 3·4·5차 연속 이월이던 가죽 옷 15 % 실측(H7)이 풀렸다** (QA 키의 효과) |
| H. 회귀 확인 | **통과 (5/5)** | 기존 루프 회귀 없음 |
| I. 데이터 조정 가능성 | **통과 (6/6)** | 정적 검증 (코드 경로 검수 + 검산 — 사용자 위임 수행) |

**1차 판정**: 6차 목표 문장 — "낚아채면 놓을 때까지 매달려 있고, 든 채로 이동해 아군 앞이나
열차 바퀴에 넘겨 처치한다" — 이 **종단으로 성립**했다 (A·B·C 코어 통과). 잔여는 주변부:
F 4건(사막 조건) · G 19건(이월 — 조건·시간) · C3·C4 · B19 재확인 · 버그 1번 수정 재확인.

## 발견된 버그

1. 🔴 **투척 좌클릭 시 클라이언트 반응 지연** (B 구역 검증 중 발견): 게스트 홀더가 좌클릭하면
   호스트 화면이 먼저 움직이고 게스트 화면이 나중에 따라온다.
   **원인**: 투척은 서버 확정 전까지 클라 화면에 아무 반응이 없다 — 좌클릭 → RPC 왕복(RTT) 후
   서버가 비행을 시작하고, 그 위치가 스냅샷 보간(0.18 s)으로 돌아와야 보인다. 발사(Fire)에는
   로컬 훅 선반영이 있는데 투척에는 없어서 생긴 비대칭.
   **1차 수정 (2026-08-10)**: 발사와 같은 규약의 로컬 선반영
   (`IHoldAttachable.BeginPredictedThrow`) — 좌클릭 즉시 로컬로 같은 수식의 비행을 재생하고
   서버 확정(기절 복제·디스폰·안전 시한 1.5 s) 도착 시 동기화 표시로 복귀.
   ❌ **재확인 실패 (2026-08-10)** — 증상 그대로. **원인**: 예측이 `transform.position`에서
   **증분**으로 전진하는데, 매 프레임 Update 단계의 스냅샷 보간(`ClientInterpolate`)이 위치를
   옛 위치로 되돌려 놓아 **예측이 누적되지 않았다** (보간 위치 + 한 걸음에 머묾). 파지 부착은
   매 프레임 절대 앵커를 다시 계산해 이 문제가 없었지만, 증분 방식인 예측은 당했다.
   **2차 수정 (2026-08-10)**: ① 예측 위치를 **내부 상태로 누적**하고 매 프레임 통째로 대입한다
   (착지 대기 중에도 — 보간이 되돌려 놓는 것을 항상 이긴다) ② 시작점을 스냅샷 위치가 아니라
   **화면에 보이는 로컬 파지 앵커**(같은 `ComputeHoldAnchor`)로 잡아 시각적 끊김을 없앤다.
   ✅ **재확인 통과 (2026-08-10)** — 투척 지연 해결 확인 (사용자).

2. 🔴 **기절 몬스터가 지상에서 컨베이어에 실리지 않는다** (재확인 중 발견): 땅바닥에 놓인 기절
   몬스터가 제자리에 박혀 있어, 함께 흘러가는 자원 아이템들과 프레임 소속이 어긋나 보인다.
   **원인**: `MonsterAgent.ServerSimulate`가 기절 중 수평 속도를 0으로 둔다 — 추격 중 지상
   몬스터는 조향 계산에 컨베이어 변위가 가산되지만, 기절체는 그 경로를 타지 않는다.
   ✅ **수정 완료 (2026-08-10)**: 기절 중 **지상(갑판 아님)이면 컨베이어 변위**
   (`Vector3.back × 스크롤 속도`)를 적용한다 — 자원 아이템들과 함께 뒤로 밀린다. 갑판 위 기절은
   열차 프레임 소속이라 그대로다. 실려 가는 동안 방향은 돌리지 않고, 깨어나면 추격이 다시
   따라잡으며(이동속도 > 스크롤 속도), 너무 뒤처지면 기존 도주 회수(60 m)로 정리된다.
   **재확인**: 지상에 놓은 기절 몬스터가 자원 노드들과 같은 속도로 흘러가는지, 깨어나면 다시
   추격해 오는지.

## 개선 방향

(2차 검증 후 기입)

**6차 비범위 이월 (7차 이후 대상 — 계획 문서 §0 결정)**

- **온도 축** — 보온 장비 기본 체온 상향 + 지역 무관 온도 (5차 개선 5·6번, 지역 에셋·평형 수식 동반)
- 낙하 위치 2건 (5차 A3 열차 위 낙하 · A6 취소 순간이동 — 5차 개선 2번)
- 버리기 UX 재설계 · 창고 파괴 시 내용물 낙하 · 몬스터 갑판 장애물 회피 (3차부터 이월)
- 2차 이월 연출 9건 (분쇄 연출 포함) · 터렛/거치 기관총 · 무게 3 대형 변종

## 원하는 방향 (사용자 원문, 2026-08-10 — **전부 반영 완료**)

세 건 모두 2차 반영으로 구현됐다 — B 구역 명세(B1·B3·B5·B7·B14·B16~B20)와 기준 수치 표가
갱신본이다. 원문 보존:

> * 몬스터를 파지한 상태에서 집게 무기를 그대로 유지한채 좌클릭 하면, 집게를 다시 발사하면 좋겠다.
> 몬스터 파지를 그대로 유지한 상태이며, 땅이든 벽이든, 어딘가에 부딛혔을 때 몬스터에게 큰 대미지 + 기절시키면 좋겠다.
> 재발사는 사거리가 길지 않으면 좋겠다. 우클릭은 몬스터를 놓기.
>
> * 현재 몬스터를 집게로 파지한 상태에서 무기를 집게에서 다른 무기로 바꿀 때 집게가 취소된다. 무기를 바꿔도 취소가 되지 않으면 좋겠다.
>
> * 현재 파지 상태에서 이동할 때 파지되어있는 몬스터가 마치 뒤따라오는 것 처럼 느껴진다. 내가 원하는 것은, 플레이어 화면 좌측 앞에 고정되는 느낌을 원한다. 따라서 집게로 끌고 도착한 상태는 다같이 하나의 오브젝트처럼 이동하는데 문제 없어야한다.

**반영 내역** (2026-08-10):

1. **파지 중 좌클릭 = 투척(슬램)** — 든 몬스터를 조준 방향으로 발사한다 (서버 확정).
   지형·구조물 충돌 시 **큰 피해 60 + 놓임(기절 2.5초)**, 미명중이면 사거리 끝에서 놓임(기절만).
   사거리는 **8 m로 짧게** — 속도·사거리·피해·판정 반경 전부 `HarpoonSettings` 에셋.
   우클릭 놓기는 그대로 (→ B5·B16~B20)
2. **무기를 바꿔도 놓지 않는다** — "놓기 ② 무기 교체" 자동 취소를 철회했다. 파지·로프가 유지되고
   든 채 다른 무기로 쏠 수 있다. 놓기·투척은 집게로 돌아와서 한다 (→ B7)
3. **화면 좌측 앞 고정 + 한 몸 이동** — 앵커에 측면 오프셋(왼쪽 0.8 m, `_holdSide`)을 더하고,
   표시를 스냅샷 보간(0.18 s 지연 — "뒤따라오는" 느낌의 원인)에서 **전 피어 로컬 부착**으로
   바꿨다: 각 피어가 자기 화면의 홀더 위치 기준으로 같은 앵커 계산(`ComputeHoldAnchor`)을 매
   프레임 대입한다. 소유자 화면은 지연 0, 원격 화면도 홀더에 붙어 함께 움직인다.
   판정 위치는 서버가 같은 계산으로 확정한다 (→ B1·B3·B14)