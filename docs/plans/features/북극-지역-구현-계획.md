# 북극 지역 구현 계획 — 얼음과 바다, 그리고 대자연

> **상태**: **1차 완료 (2026-08-30) · 2차 진행 중**
> **확정 범위**: **1~4차** (원경 → 근경 → 물·얼음 → 검증 36건)
> **위치**: [레벨 디자인 구현 계획 §3 4차](레벨-디자인-구현-계획.md)의 북극 몫을 별도 문서로 떼어낸 것.
> [바다](바다-지역-구현-계획.md)·[사막](사막-지역-구현-계획.md)이 같은 방식으로 먼저 떨어져 나갔다.
> **기준 커밋**: `feat/region-biomes` = `e12312a`(착수 시점, 사막 1~4차 완료) · EditMode 기준선 **1,202/1,202**

> **이 계획이 하는 일**: 북극을 **얼음과 바다가 교차하는 대자연**으로 세우고, 1회차 이후
> **36건이 잔여로 남은 M7 3차 검증**을 그 화면 위에서 마감한다.
>
> **이 계획이 하지 않는 일**: 동상·난방 2단·정수 사슬·북극 보스의 **재설계** — 그 축은
> [M7 3차 구현 계획](../M7/M7-3차-구현-계획.md)이 소유하고, 이 문서는 검증하고 정정만 한다.
> 유적 최종장(기획서 §8.3)도 밖이다 — 북극은 그 **복선만** 놓는다.

---

## 0. 이 계획이 만들어진 경위

북극은 **시스템이 화면보다 먼저 선 유일한 지역**이다.

| 지역 | 시스템 | 화면 | 상태 |
|---|---|---|---|
| 숲 | 기본 | 팔레트 10종 + `M_Sky_Forest` | 둘 다 성립 |
| 사막 | 모래폭풍 | 단일 타일 | **대자연 계획 착수 대기** |
| 바다 | 수영·잠수·낚시 | 팔레트 10종 + `M_Sky_Sea` | 둘 다 성립 (4차까지) |
| 대초원 | 스탬피드 | 단일 타일 | 미착수 |
| **북극** | **동상 · 방한 4부위 · 정수 사슬 · 난방 2단 · 보스 · 날씨 2종** | **단일 타일 · 하늘 없음** | **시스템이 가장 두껍고 화면이 가장 얇다** |

M7 3차가 북극 시스템을 한 차수에 다 넣었고(2026-08-14), 1회차 검증에서 W1~W6 후속 조치가
나왔지만 **판정 36건이 아직 잔여**다. 그 사이 바다가 Day 10~12에 끼어 **북극이 Day 14 → Day 17로
밀렸고**, QA 키 매핑도 바뀌었다 — **검증 문서의 재현 수단이 낡아서 지금 그대로는 재개할 수 없다**(§3.4).

### 0.1 축이 두 번 바뀌었다 (2026-08-29, 사용자 지시)

| 지시 | 계획에 미친 영향 |
|---|---|
| *"대자연이 느껴지는 게 핵심 · 맵이 넓으면 좋겠다 · 저 멀리 산맥"* | 축이 **팔레트 → 원경**으로. 사막 계획과 같은 **3층 구조**를 쓴다(§4) — 1차가 근경이 아니라 원경이 된다 |
| *"땅은 얼음 + 바다 · 얼음에서는 미끄러지고 · 바다에서는 급격한 온도 감소 + 이속 저하"* | **지형 컨셉 자체가 바뀌었다.** 설원 육지가 아니라 **유빙과 바다가 교차하는 북극해**다(§5). 물리 축 3종(수영·미끄러짐·침수)이 신설돼 **3차가 생겼다** |
| *"얼음과 바다가 공존하며 · 비율이 번갈아 · 얼음 우세도 사이사이 물길"* (2026-08-30) | 초안의 **"±8 m 한 줄 얼음길"이 오독이었음이 드러났다.** 교차하는 것은 폭이 아니라 **조각의 크기와 밀도**이고, 물길은 얼음 우세 구간에도 촘촘히 들어간다(§5.1·§5.2) |

---

## 1. 확정 결정 10건 (2026-08-29, 사용자 결정)

| # | 항목 | 결정 |
|---|---|---|
| ① | **지형 컨셉** | **얼음이 주인 구간 ↔ 바다가 주인 구간의 교차** |
| ② | **무엇이 교차하나** | **얼음 조각의 크기와 밀도** — 얼음 우세(큰 판 + 물길)와 바다 우세(작은 유빙이 흩어짐)가 번갈아 온다 |
| ③ | **침수 처벌** | **중간 — 탈출 가능.** 이속 ×0.7 · 체온 하락 가속. 실수 1회로 죽지는 않는다 |
| ④ | **물면 깊이** | **−1.5 m · 맨틀로 기어오른다** (`LadderClimbLogic.ComputeMantleMotion` 재사용) |
| ⑤ | **원경** | **사막과 같은 3층 구조** — `DistantSceneryLayer`(사막 1차 신설)를 **재사용**한다 |
| ⑥ | **안개** | **지역별 fog 신설 · 북극은 옅게** (`RegionDefinition` fog 필드 — 사막이 신설하는 것을 공유) |
| ⑦ | **오로라** | **품질 우선 — 하늘 셰이더 대역 + 시차 커튼 2겹.** *"아름답게 표현할 수 있는 방식으로, 중요하다"* |
| ⑧ | **눈 표현** | **파티클 + 화면 서리 오버레이** (동상 결빙과의 구분 규칙은 §3.3) |
| ⑨ | **QA 키** | **F5 = 다음 지역 첫날 아침 점프 신설** |
| ⑩ | **차수 편성** | **1차 원경 → 2차 근경 → 3차 물·얼음 → 4차 검증** |
| ⑪ | **북극 보스** | **이 계획 밖 — 규격만 보관.** 사막 계획 §9와 같은 처리 (§11 리스크 7) |
| ⑫ | **얼음낚시** | **열되 어렵게** — 입질 ×4 · 이중 어획 0 · 얼음 위에서는 못 던진다 (§8.3) |
| ⑬ | **열차 지붕 결빙** | **기능은 만들되 지붕 적용은 보류** — 마찰을 표면 데이터로 두어 나중에 값만 넣으면 켜진다 (§5.5) |
| ⑭ | **비적대 생물** | **순록 → 바다표범.** 유빙 위에 누워 있다가 다가가면 물로 들어간다 (§7.1.1) |
| ⑮ | **착수 순서** | **사막 먼저** — `DistantSceneryLayer`·지역 fog를 사막 1차가 소유하고, 북극 1차는 **에셋 배치만** |
| ⑯ | **궤도 밑 얼음** | **±8 m 띠는 어느 구간에서도 끊기지 않는다** — 하차·앵커·수리가 항상 가능 (§5.1) |
| ⑰ | **물길** | **얼음 우세 구간에도 촘촘히.** 폭을 점프 거리(걷기 3.1 · 달리기 4.85 m)로 등급화 (§5.2) |

---

## 2. 실측 기준선 — 이 계획이 쓰는 자

### 2.1 월드

| 항목 | 값 | 출처 |
|---|---|---|
| 타일 길이 · 폭 | 40 m · **120 m (±60)** | `WorldScrollSettings` · `TerrainTile.prefab` |
| 스크롤 속도 | **6 m/s** | `_baseScrollSpeed` |
| 카메라 far clip | **1,000 m** | `Game_ArtTest.unity` |
| 북극 주행 | 390 s/일 × **3일** = 1,170 s → **7,020 m = 176타일** | 레벨 가이드 §2.2 |
| 궤도 통로 | \|x\| ≤ **3.3** — 지형 전면 금지 | 가이드 §4.2 |
| 몬스터 주행 대역 | **4 ≤ \|x\| ≤ 24** · 연속 장벽 8 m 초과 금지 | 〃 |

### 2.2 플레이어 이동·수영 (`PlayerMovementSettings`)

| 항목 | 값 | 이 계획에서의 쓰임 |
|---|---:|---|
| 걷기 / 달리기 | 4.5 / **7 m/s** | 미끄러짐 제동 거리의 입력 |
| 점프 높이 | **1.2 m** | **물면 −1.5를 못 넘는다** → 맨틀 필요의 근거 |
| 공중 가속 × 제어 | 20 × 0.5 = **10 m/s²** | 얼음 접지 가속의 비교 기준 |
| 수영 속도 | **3.5 m/s** | 깊은 물에서 스크롤 6 m/s를 못 이긴다 |
| 수영 진입 / 이탈 깊이 | **1.0 / 0.2 m** | **얕은 물 설계의 핵심 자** (§5.3) |
| 잠김 항력 시작 / 완전 | 1.8 / 3.5 m | 얕은 물에는 걸리지 않는다 |

### 2.3 북극 지역 데이터 as-built (`Region_Arctic.asset`)

| 필드 | 값 | 비고 |
|---|---:|---|
| 일수 | 3 | Day **17~19** (바다 편입 후) |
| 웨이브 / 체력 배율 | 1.7 / 1.5 | 다섯 지역 중 최대 |
| 낮 / 밤 온도 | **−18 / −32 ℃** | |
| 날씨 확률 | **0.7** — 폭설(fog 0.05 · 집게 ×0.55) · 혹한파(fog 0.02 · **−10 ℃**) | 낮 한정 |
| 자원 | 얼음 0.45 · 희귀 금속 0.30 · 유적 부품 0.15 · 고철 0.10 / 배율 **2.5**(30 m 간격 · 타일당 **1.33개**) | 전용 메시 3종 **반입 완료** |
| 보스 | `BossDefinition_Arctic` + `Boss_Arctic` | 해시 정규화 완료 |
| **`_segmentPalette`** | **없음** | |
| **`_skyboxMaterial`** | **없음** | |
| **`_hasWater`** | **없음 (= false)** | **여기를 켜는 것이 §5의 출발점** |

### 2.4 아트 재고

| 묶음 | 재고 | 북극에서의 쓰임 |
|---|---|---|
| `Environment/Common/` 11종 | 바위 3 · 노두 · 자갈 · 침목 · 신호기 · 이정표 · 울타리 · 새떼 | **백색 재도색으로 그대로** — 눈에 덮이면 원래 무엇이었는지가 지워진다 |
| `Environment/Forest/` 15종 | 절벽 2 · 경사 · 강둑 · 교각 | 절벽·경사는 **빙벽 절개면**으로 재도색 |
| **북극 전용** | **0종** | 신규 반입 4종만 (§7.2) |
| 자원 노드 | `Res_Ice` · `Res_RareMetal` · `Res_RelicPart` | **이미 있다** |

### 2.5 이미 서 있어서 이 계획이 건드리지 않는 것

동상(`PlayerFrostbite`/`FrostbiteMath`) · 방한 4부위 · 정수 사슬 · 난방 2단 · 북극 보스 ·
날씨 2종 · 자원 4종. **전부 M7 3차 산출물이고, 이 계획은 검증만 한다.**

---

## 3. 실측이 찾아낸 어긋남 4건 ⚠

### 3.1 물은 코드 0줄로 열리는데, 바닥은 그렇지 않다

`NetworkPlayerController.TryGetWaterSurfaceY`가 `RegionDefinition.HasWater`를 보고,
주석이 *"그 지역들은 `HasWater`가 꺼져 있어 이 경로가 통째로 비활성이다"* 라고 적어 뒀다.
**`_hasWater: 1` + `_waterSurfaceY` 두 줄이면 수영·잠수·물 항력이 그대로 성립한다.**

몬스터도 마찬가지다 — `MonsterAgent.ResolveSurfaceY`·`BossAgent`가 같은 필드를 보고
*"물 지역이면 몬스터도 물면에 선다"*를 이미 구현했다(바다 4차).

> **그런데 `SurfaceY`는 지역당 하나다.** 바다는 사방이 물이라 문제가 없었지만,
> **얼음(y 0)과 물(y −1.5)이 교차하면 이 단일값이 깨진다** — 얼음 구간에서 몬스터가
> 1.5 m 묻힌 채 걸어온다. **3차에 발밑 레이캐스트 보정 1건이 필요하다**(§8.4).

### 3.2 깊은 물에서는 헤엄쳐도 열차를 못 따라간다 — 그래서 얕은 물을 만든다

바다 계획 §6.1이 이미 계산해 뒀다: *"감쇠가 없으면 수영 속도가 스크롤을 못 이겨 뛰어드는 순간
복귀가 불가능해진다."* 북극에서 침수 이속 ×0.7까지 걸면 **3.5 → 2.45 m/s 대 스크롤 6 m/s**다.

| 구간 | 순 전진 | 결과 |
|---|---:|---|
| 깊은 물(수영) + ×0.7 | 2.45 − 6 = **−3.55 m/s** | 22초면 열차 후미(77.7 m)를 벗어난다 — **사실상 사망** |
| 얕은 물(걷기) + ×0.7 | 4.5 × 0.7 = **3.15 m/s**, 컨베이어가 몸을 실어 나른다 | 물속에서도 **뒤로 밀리지 않는다** |

**결정 ③("중간 — 탈출 가능")은 얕은 물을 요구한다.** 그 수치가 §5.3이다.

### 3.3 서리 오버레이와 동상 결빙이 같은 자리를 쓴다

결정 ⑧의 화면 서리는 **`PlayerFrostbite`의 화면 결빙 오버레이와 화면상 같은 표현**이다.
겹치면 플레이어가 **동상 경고를 날씨로 오인**한다 — 검증 C 구역(동상 10건)이 그대로 무의미해진다.

> **구분 규칙**: 날씨 서리는 **화면 가장자리 · 무채색 · 정지**, 동상 결빙은 **중앙까지 침범 ·
> 청색조 · 맥동**. 둘이 동시에 걸리면 **동상이 위에 그려진다**. 이 규칙을 4차 C 구역의 판정 항목으로 넣는다.

### 3.4 검증 문서의 재현 수단이 낡았다

| 항목 | 문서 | 현재 |
|---|---|---|
| 북극 진입 Day | Day 14 | **Day 17** |
| 최속 진입 | 넘패드 3 × 13회 | 넘패드 3 × **16회** → **F5 × 4회**(결정 ⑨) |
| 자원 지급 키 | 넘패드 **9** | 넘패드 **4** (9는 지금 샘플 데미지 30) |

---

## 4. 대자연 — 원경 3층 구조 ★

사막 계획 §4와 **같은 골격**이다. 층·거리·시차계수 규격은 그쪽이 소유하고, 여기서는
**북극에 배정한 대상과 값**만 적는다.

### 4.1 층 배정

| 층 | 거리 | 흐름 | 소유자 | **북극의 내용** |
|---|---|---:|---|---|
| 근경 | 0 ~ 60 m | 6 m/s | 타일 스트리밍 | 얼음·물길·유빙 조각 팔레트 10종 (§7) |
| 중경 | 60 ~ 300 m | 6 m/s | 타일 지면판 | 유빙 실루엣 · 압력 능선(얼음이 밀려 솟은 등성이) |
| **원경** | 300 ~ 800 m | **0.12 ~ 0.6 m/s** | **`DistantSceneryLayer`** | **빙하 산맥 · 빙산군 · 오로라 커튼 · 원경 해면판** |

### 4.2 fog — 북극은 **맑은 날에만** 맑다

**북극 전용 값: `ExponentialSquared` · density `0.0017` · 색 `#DCE8F0`(창백한 청백).**

| 거리 | 현재 씬 0.0062 | **북극 0.0017** | 사막 0.0015 |
|---:|---:|---:|---:|
| 300 m | 3.1 % | **77 %** | 82 % |
| 500 m | 0.7 % | **49 %** | 57 % |
| 800 m | ~0 % | **16 %** | 24 % |

사막보다 **약간 짙게** 잡았다 — 찬 공기의 미세 결정이 만드는 뿌연 층은 북극의 정체성이고,
800 m 산맥이 16 %로 **희미하게 걸리는 것**이 오히려 거리감을 키운다.

> **소유권** — 사막 §4.2와 같다: `WeatherVisualController`의 날씨 종료 처리를
> *"씬 값 복원"*이 아니라 **"현재 지역 값 재적용"**으로 바꾼다. 북극은 날씨 확률 0.7이라
> **이 정리를 안 하면 첫 폭설이 끝나는 순간 지역 fog가 영구히 사라진다** — 사막(0.6)보다 더 자주 터진다.

### 4.3 시차 레이어에 놓는 것 (3일 = 1,170 s 기준)

| 대상 | 거리 | 시차계수 | 실효 속도 | wrap 간격 | 재등장 주기 | 북극 3일 중 |
|---|---:|---:|---:|---:|---:|---:|
| **빙하 산맥 능선** | 800 m | 0.03 | 0.18 m/s | 1,200 m | 6,667 s | **0회 — 반복이 안 보인다** |
| **빙산군** (탁상형 빙산 3~4기) | 500 m | 0.10 | 0.60 m/s | 400 m | 667 s | **약 1.75회** |
| **오로라 커튼** (결정 ⑦) | 600 m | **0.02** | 0.12 m/s | 2,000 m | 16,667 s | **0회** — 하늘은 따라오지 않는다 |
| 원경 해면판 | — | 0 | 0 | — | — | 균질하므로 흐를 필요가 없다 |

> **산맥이 3일 내내 한 번도 반복되지 않는다.** 팔레트가 133초마다 반복되는 것과 대비된다.

### 4.4 오로라 — 2겹 (결정 ⑦, 품질 우선)

| 겹 | 구현 | 성질 |
|---|---|---|
| **하늘 대역** | `S_RegionSky`에 오로라 밴드 추가 (UV 스크롤 + 색 진동) | **fog의 영향을 받지 않는다** — 폭설로 시야가 20 m로 닫혀도 하늘은 살아 있다. 비용 **0 tris** |
| **시차 커튼** | 반투명 커튼 메시 2~3장 · 계수 0.02 | 지형 뒤로 지나가는 **깊이**를 만든다. fog를 받아 날씨에 지워진다 |

두 겹이 함께 있을 때만 *"하늘에 걸려 있으면서도 저 뒤에 있는 것"*이 된다.
**세계관 §4.4의 복선** — 오로라 = 별씨의 궤적 — 이 여기 실린다.

> **정렬 주의**: 반투명 커튼은 스카이박스보다 앞, 지형보다 뒤여야 한다. 렌더 큐 고정을
> 1차 완료 기준에 넣는다 — 순서가 틀리면 커튼이 산맥을 지운다.

### 4.5 원경 해면판 — 판 끝 문제의 해답

fog를 옅게 하면 타일 지면 끝(x ±60)이 드러난다. 사막은 모래판 1장으로 덮었고,
**북극은 그 자리에 해면판을 깐다** — 유빙 바깥은 원래 바다이므로 **설정과 정확히 맞는다**.
±800 m 평면 1장 · y −1.5(물면과 같은 높이) · 시차계수 0 · **12 tris**.

### 4.6 예산

| 오브젝트 | tris |
|---|---:|
| 원경 해면판 | 12 |
| 빙하 산맥 능선 (3조각) | ~1,500 |
| 빙산군 (탁상형 4기) | ~800 |
| 오로라 커튼 (3장) | ~200 |
| **합계** | **~2,500** |

활성 타일 9장 목표 270,000 tris의 **0.9 %**다. **대자연의 비용은 폴리곤이 아니라 배치와 fog 값이다.**

---

## 5. 얼음과 바다 — 공존 설계 ★

> **이 절이 북극을 다른 네 지역과 가르는 지점이다.**

### 5.1 교차하는 것은 폭이 아니라 **얼음 조각의 크기와 밀도**다 (결정 ①·②·⑯)

**얼음과 바다는 어느 구간에서든 함께 있다.** 달라지는 것은 얼음이 얼마나 크고 촘촘한가뿐이다.

```
얼음 우세 — 얼음 땅이 대부분, 사이사이 물길
██████████████████████████████████████
████████≈≈≈██████████████≈≈≈██████████
██████████████████████████████████████
████[══════════ 열 차 ══════════]█████
█████████████████≈≈≈██████████████████
███████≈≈≈████████████████████████████

바다 우세 — 유빙이 작게 흩어지고 바다가 주인
≈≈≈≈███≈≈≈≈≈≈≈≈≈██≈≈≈≈≈≈≈≈≈≈≈≈████≈≈≈
≈≈≈≈≈≈≈≈≈≈████≈≈≈≈≈≈≈≈≈███≈≈≈≈≈≈≈≈≈≈≈
██████████████████████████████████████  ← 얼음 띠 ±8 m
███[══════════ 열 차 ══════════]██████     항상 연속
██████████████████████████████████████
≈≈≈≈≈≈≈≈≈██≈≈≈≈≈≈≈≈≈≈≈≈≈████≈≈≈≈≈≈≈≈≈
```

| 구간 | \|x\| ≤ 8 (궤도 띠) | 8 < \|x\| ≤ 60 | 팔레트 비중 |
|---|---|---|---:|
| **얼음 우세** | 연속 얼음 | **큰 얼음판 + 물길 2~4줄** | 5종 (0.65) |
| **전이** | 연속 얼음 | 판이 갈라지며 조각이 작아진다 | 2종 (0.17) |
| **바다 우세** | 연속 얼음 | **작은 유빙이 흩어지고 물이 넓다** | 3종 (0.18) |

**궤도 밑 ±8 m 얼음 띠는 어느 구간에서도 끊기지 않는다** (결정 ⑯). 이 하나가 세 가지를 지킨다 —
열차에서 내리면 발 딛을 곳이 있고(하차 대역 규약), 자원 앵커를 놓을 자리가 있으며,
수리·건축이 어디서든 된다. **위험은 띠를 벗어날 때 생긴다.**

> **몬스터 주행 대역(4 ≤ \|x\| ≤ 24)이 흩어진 조각 위다.** 이것이 결함이 아니라 이득이다 —
> 몬스터는 **조각을 발판 삼아 띠까지 건너온다**. 바다 4차의 물면 주행·수면 도약이 그대로 쓰이고,
> 바다 우세 구간의 웨이브는 **물과 조각을 번갈아 밟고 오는 그림**이 된다.

### 5.2 물길 — 넘을 것인가 돌아갈 것인가 (결정 ⑰)

**얼음 우세 구간에도 물길이 촘촘히 지난다.** 통짜 얼음판은 A 세그먼트 하나뿐이고,
나머지는 전부 물길을 낀다. 폭은 **점프 수평 거리**를 자로 삼아 나눈다.

체공 시간 = `2√(2h/g)` = `2√(2×1.2/20)` = **0.69초**.

| 물길 등급 | 폭 | 판정 | 역할 |
|---|---:|---|---|
| 실개 | 1.5 m | 걸어서 넘는다 | 장식 — 얼음이 통짜가 아님을 보여준다 |
| **좁은 물길** | **3 m** | 걷기 점프 **3.1 m**로 아슬아슬 | 대부분의 물길. 매번 작은 판단이 걸린다 |
| **넓은 물길** | **5 m** | **달려야** 넘는다 (7 m/s → 4.85 m) | 달릴지 말지 — 그런데 달리면 미끄러진다(§5.4) |
| 열린 물길 | 8 m 이상 | 못 넘는다 | 헤엄치거나 우회. 자원이 건너편에 있으면 대가를 치른다 |

> **이 표가 §5.4와 정면으로 맞물린다.** 넓은 물길을 넘으려면 달려야 하는데, 얼음에서 달리면
> **8.17 m를 미끄러져 물길 앞에서 못 멈춘다.** 넘든지 빠지든지 둘 중 하나가 되는 것이다 —
> "미끄러짐"이 단순한 조작 불편이 아니라 **결정을 강요하는 장치**가 되는 지점이 여기다.

### 5.3 구간이 이어지는 길이 — 기준안 87초

`SegmentPickLogic`은 지금 **타일 한 장마다 독립 추첨**이라, 그대로 두면 얼음 우세와 바다 우세가
**6.67초마다 뒤바뀐다** — 교차가 아니라 뒤죽박죽이다. **구간으로 묶는다.**

| 한 바퀴 | 구성 | 길이 | 북극 3일(1,170 s) 중 |
|---|---|---:|---:|
| **기준안** | 얼음 6 + 전이 1 + 바다 5 + 전이 1 = **13장** | 520 m · **87초** | **13.5바퀴** |

낮 240초에 2.8바퀴 · 밤 150초에 1.7바퀴 — **밤 하나가 두 지형을 다 거친다.**
바다 우세 구간에 열차(77.7 m)가 통째로 들어가 있는 시간은 **27초**다.

**구현**: 타일 인덱스를 바퀴 길이로 나눠 **구간 군을 먼저 뽑고, 군 안에서 세그먼트를 뽑는 2단 추첨**.
시드는 지금처럼 인덱스에서 나오므로 **결정론은 그대로**다(전 피어 동일).

> 87초가 긴지 짧은지는 달려 봐야 안다 — **2차 실측 조정 항목**으로 표시한다.

### 5.4 물면 −1.5 m와 두 종류의 물 (결정 ③·④)

**물면은 지역당 하나(−1.5)이고, 바닥이 세그먼트마다 다르다.** 이 한 가지가 처벌 구배를 만든다.

| 물 | 바닥 | 발 기준 잠김 | 판정 | 처벌 |
|---|---:|---:|---|---|
| **얼음 틈 (얕은 물)** | **y −2.3** | **0.8 m** | 진입 깊이 1.0 **미만 → 걷기 유지** | 이속 ×0.7 · 체온 급락 · 얼음 벽 1.5 m를 **맨틀**로 오른다 |
| **열린 물길 · 조각 사이 (깊은 물)** | 없음 (−6 이하) | 1.0 이상 | **수영** | 위 + **스크롤에 쓸려 열차를 놓친다**(§3.2) |

> **점프 높이 1.2 m가 얼음 두께 1.5 m를 못 넘는 것이 설계의 핵심이다.**
> 빠지면 반드시 맨틀 동작을 거친다 — *"탈출 가능하되 몇 초 걸린다"*가 결정 ③의 정의다.
> 여유가 0.2 m뿐이므로 **1차가 아니라 3차에 실측으로 확정**한다(`CharacterController` skinWidth 0.08 포함).

### 5.5 얼음 위에서 미끄러진다

`PlayerMotor.ComputeHorizontalVelocity`는 **접지면 `desiredVelocity`를 즉시 반환한다** — 마찰이 무한이다.
공중 경로는 이미 `MoveTowards(10 m/s²)`이므로, **얼음은 접지 분기에 같은 형태를 낮은 가속으로 넣는 것**이다.

| 표면 | 접지 가속 | 걷기 4.5 제동 거리 | 달리기 7 제동 거리 |
|---|---:|---:|---:|
| 눈 덮인 유빙 (기본) | **12 m/s²** | 0.84 m | 2.04 m |
| **맨 얼음 (빙판)** | **3 m/s²** | **3.38 m** | **8.17 m** |

> **넓은 물길(5 m)을 넘으려면 달려야 하는데, 달리면 8.17 m를 미끄러져 그 앞에서 못 멈춘다.**
> 미끄러짐이 조작 불편이 아니라 **결정을 강요하는 장치**가 되는 지점이다(§5.2) —
> 지금까지 없던 **속도 관리** 축이 생긴다.

표면 종류는 `WorldFrameSurface`(이미 타일에 붙어 있는 컨베이어 표시자)에 필드 하나를 더해 읽는다 —
**새 마커를 만들지 않는다.**

> **열차 지붕은 이번에 얼지 않는다** (결정 ⑬). 미끄러짐을 `WorldFrameSurface`에 얹으면
> 지붕은 그 경로를 타지 않아 **자동으로 제외**된다. 다만 마찰을 표면 데이터로 두었으므로,
> 나중에 지붕에 값을 넣기만 하면 켜진다 — **구조는 열어 두고 적용만 보류한다.**

### 5.6 이 절이 요구하는 코드 변경 6건

| # | 변경 | 파일 | 규모 |
|---|---|---|---|
| 1 | `PlayerMotor.ComputeHorizontalVelocity`에 접지 가속 인자 | `Gameplay/Player/` | 순수 함수 · 분기 1 |
| 2 | `WorldFrameSurface`에 표면 마찰 필드 + 조회 | `Gameplay/World/` | 필드 + getter |
| 3 | `PlayerSubmersion` 신설 — `IMoveSpeedModifier` 구현 + 체온 입력 | `Gameplay/Player/` | ~80줄 |
| 4 | 몬스터 발밑 보정 (`SurfaceY` 단일값 문제, §3.1) | `Gameplay/Monsters/` | 짧은 레이캐스트 1 |
| 5 | `SegmentPickLogic` 2단 추첨 — 구간 군 → 세그먼트 (§5.3) | `Gameplay/World/` | 순수 함수 · 나눗셈 1 |
| 6 | 낚시 지역 배율 + 지형 차폐 판정 (§8.5) | `Gameplay/World/` | 필드 + 레이캐스트 1 |

**3번이 확장점 그대로 들어간다** — `IMoveSpeedModifier`가 *"다른 축은 이 계약을 구현하는 것만으로
같은 지점을 재사용한다(OCP)"*로 설계됐고, 배율은 곱해진다. **동상 ×0.8 × 침수 ×0.7 = ×0.56**이
컨트롤러 수정 없이 성립한다. 체온도 `PlayerTemperature.GetRegionAmbient`가 유일한 입구다.

---

## 6. 1차 — 대자연 원경

### 6.1 산출

1. `M_Sky_Arctic.mat` — 낮이 거의 없는 어스름 (천정 짙은 청보라 · 수평선 창백한 흰색)
2. `S_RegionSky` **오로라 밴드 확장** (결정 ⑦ 1겹)
3. `Region_Arctic`에 fog 3필드 = `0.0017` · `#DCE8F0` (사막이 신설하는 필드를 공유 — §11 리스크 1)
4. `DistantSceneryLayer` 배치 — 빙하 산맥 · 빙산군 · 오로라 커튼 · 원경 해면판 (§4.3)
5. **F5 = 다음 지역 첫날 아침 점프** QA 키 (결정 ⑨) + `qa-debug-keys.md` 갱신
6. 진입 스모크 — 검증 **H(네트워크 등록 회귀)·A(지역 등재)**를 여기서 함께 확인

### 6.2 완료 기준

- Day 16 → **Day 17**에 지역 배너가 "북극"으로 바뀌고, **지평선에 빙하 산맥이 걸려 있다**
- 산맥이 열차보다 **10배 이상 느리게** 흐르는 것이 체감된다 (근경 6 m/s vs 0.18 m/s)
- 오로라가 **폭설 중에도 하늘에 남는다** · 커튼이 산맥을 지우지 않는다(렌더 큐)
- 지면 판 끝(x ±60)이 해면판에 이어져 **끊긴 자리가 보이지 않는다**
- 폭설이 끝난 뒤 **지역 fog가 0.0017로 되돌아온다** (§4.2 소유권 — 확률 0.7이라 즉시 드러난다)
- EditMode 회귀 0 · `NetworkObject` 증가 0

### 6.3 as-built (2026-08-30) ★

**첫 작업이 답한 것** (§14 · 리스크 1) — 사막은 **1차가 아니라 4차까지** 서 있었다.
`DistantSceneryLayer`·`RegionFogController`·`IRegionFogProvider`·`RegionDefinition` fog 5필드가
전부 완성돼 있었고, `Region_Arctic`에는 이미 **`_overridesFog: 1` + 낮·밤 두 벌 모두 씬 값(0.0062)**이
배선돼 있었다(사막 1차의 회귀 방어선). 그래서 북극 1차의 fog 일감은 **값 4개를 북극 값으로 바꾸는 것**뿐이었고,
계획이 예상한 "필드 신설"은 한 줄도 필요 없었다.

#### 원경 층 — 4층이 아니라 **5층**이다

| 층 | 시차 | 되감기 | 실효 속도 | 되감기 주기 | 내용 | 렌더러 |
|---|---:|---:|---:|---:|---|---:|
| `SeaPlate` | **0** | — | 0 | ∞ | 원경 해면판 ±1,200 m · **윗면 y −1.5** · 12 tris | 1 |
| `MidScenery` | **0.35** | 300 m | 2.10 m/s | 142.9 s | 압력 능선 3 + 유빙 2 × 5주기 · 대역 \|x\| 105~225 m | 25 |
| `IcebergScenery` | **0.10** | 400 m | 0.60 m/s | 666.7 s | 탁상형 빙산 7기 × 4주기 · 대역 \|x\| 230~760 m | 28 |
| `RidgeScenery` | **0.03** | 1,200 m | 0.18 m/s | 6,666.7 s | 빙하 산맥 3종 × 6방위 × 3반지름 × 2벌 · **반지름 730·800·870 m** | 36 |
| `AuroraScenery` | **0.02** | 2,000 m | 0.12 m/s | 16,666.7 s | 오로라 커튼 3장 × 2벌 · 거리 600·640·680 m · 밑동 y 90 · 높이 300 | 6 |

**합계 96 렌더러 · 3,156 tris · `Collider` 0 · `NetworkObject` 0.**
활성 타일 9장 목표 270,000 tris의 **1.17 %** — 계획 §4.6 예산 ~2,500 tris를 **+26 %** 넘겼다.
초과분의 출처는 ⓐ 계획에 없던 중경 층(750 tris)과 ⓑ 능선 실루엣 3종화(§13 결정 ㉑)다.

> **왜 5층인가.** 계획 §4.1은 중경(60~300 m)의 소유자를 *"타일 지면판"*(6 m/s)으로 적었다.
> 그런데 그 표는 **사막 as-built 이전**에 쓰였고, 사막이 결정 ⑧로 **중경 시차 대역(0.35)**을
> 4층 공통 규격에 넣었다(가이드 §7.1). 계획 §4가 *"층·거리·시차계수 규격은 사막이 소유한다"*고
> 적어 두었으므로 **공통 규격 쪽을 따랐다.** 여기에 오로라 커튼이 독립 층이라 5층이 된다.

#### fog · 하늘

| 항목 | 값 | 300 m | 500 m | 800 m |
|---|---|---:|---:|---:|
| 낮 | `#DCE8F0` · **0.0017** | 77.4 % | 48.8 % | 15.6 % |
| 밤 | `#2A4257` · **0.0017** | 〃 | 〃 | 〃 |

밤 색은 계획에 없어서 **여기서 정했다**(§13 결정 ㉒). `M_Sky_Arctic` = 천정 `#293A5C` 짙은 청보라 ·
수평선 `#CCDBE6` 창백한 흰색 · 헤이즈 폭 0.30 · 노출 1.2.

#### 오로라 — 두 겹이 실제로 두 겹인가

| 겹 | 구현 | 비용 | 확인 |
|---|---|---:|---|
| 하늘 | `S_RegionSky`에 `_Aurora*` 8프로퍼티 추가 · 대역 중심 dir.y **0.34** · 폭 0.095 · 굽이 0.16 · 결 밀도 2.4 | **0 tris** | fog 0.05(폭설)에서도 그대로 남는다 |
| 커튼 | `S_AuroraCurtain` 신설 · **가산 합성(One One)** · 시차 0.02 | 192 tris | A/B 픽셀 비교: 커튼을 켜면 **밝아진 화소 338,278 · 어두워진 화소 46**(노이즈) |

> **"커튼이 산맥을 지우지 않는다"를 렌더 큐가 아니라 합성 방식으로 못박았다.** 알파 블렌딩은
> 뒤의 능선을 커튼 색으로 덮지만, 가산은 빛을 더할 뿐이라 어떤 순서로 그려도 능선이 남는다.
> 위 A/B의 "어두워진 화소 46"이 그 증거다 — 계획 §4.4의 렌더 큐 주의가 **불필요해졌다.**

#### 신규 파일

| 종류 | 파일 |
|---|---|
| 셰이더 | `S_AuroraCurtain.shader` (신설) · `S_RegionSky.shader` (오로라 대역 확장) |
| 절차 메시 5 | `Mesh_Env_GlacierRidge` 34 tris · `_B` 40 · `_C` 46 · `Mesh_Env_Iceberg` 24 · `Mesh_Env_AuroraCurtain` 32 |
| 머티리얼 6 | `M_Sky_Arctic` · `M_Env_Distant_Glacier` · `M_Env_Distant_Iceberg` · `M_Env_Distant_Floe` · `M_Env_SeaPlate_Arctic` · `M_Env_AuroraCurtain` · `M_VFX_Blizzard` |
| 프리팹 2 | `DistantScenery_Arctic` · `WeatherVfx_Blizzard` |
| 코드 3 | `RegionTimelineMath.NextRegionFirstDay` · `IRegionService.NextRegionFirstDay`(+`RegionController`) · `DayCycleController` F5 |
| 씬 | `Game_ArtTest.unity`에 프리팹 2개 배치 — **삽입 116줄, 재정렬 diff 0** |

#### 눈보라 입자 — 계획 목록 밖에서 들어온 것

`WeatherVfx_Blizzard.prefab`(입자 3층: `Flakes` 1,500/s · `Sheets` 170/s · `GroundDrift` 420/s)을
1차에 넣었다. **계획 §6.1의 6개 산출 목록에 없다.** 근거는 완료 기준 *"오로라가 폭설 중에도 하늘에
남는다"* 로, 판정하려면 폭설이 화면에 있어야 하는데 북극에는 날씨 연출이 fog뿐이었다.
사막 4차가 만든 `WeatherParticleView`를 **한 줄도 안 고치고** 재사용했다 — 담당 날씨 슬롯에
`Weather_Blizzard`를 꽂는 것으로 끝났고, 이것이 결정 ⑲(*"북극 블리자드에 모래가 날리면 안 된다"*)가
설계해 둔 그대로다. 입자 텍스처는 `T_VFX_SandGrain`을 흰빛으로 물들여 재사용했다(신규 텍스처 0).

#### 완료 기준 자체 점검

| # | 기준 | 판정 | 근거 |
|---|---|---|---|
| 1 | Day 17에 배너가 "북극" | **미검증** | Play 필요. F5 × 4 = Day 17은 EditMode가 고정 (`F5_네_번이면_북극이다`) |
| 2 | 지평선에 빙하 산맥이 걸린다 | **충족** | 에디터 렌더 — 800 m 능선이 fog 15.6 %로 남는다 |
| 3 | 산맥이 열차보다 10배 이상 느리다 | **충족(계산)** | 6 ÷ 0.18 = **33배**. 체감은 Play |
| 4 | 오로라가 폭설 중에도 하늘에 남는다 | **충족** | fog 0.05 렌더에서 하늘 겹이 그대로 |
| 5 | 커튼이 산맥을 지우지 않는다 | **충족** | 가산 합성 · A/B 어두워진 화소 46/921,600 |
| 6 | 판 끝(x ±60)이 해면판에 이어진다 | **부분 충족** | 판 끝은 덮였다. 다만 **타일 지면 y 0 ↔ 해면판 y −1.5의 1.5 m 단차**가 남는다 — 2차 팔레트가 얼음 가장자리를 세울 때까지는 흰 절벽으로 보인다 |
| 7 | 폭설 뒤 fog가 0.0017로 복귀 | **미검증(코드 경로는 기존)** | 복원 대상 변경은 사막 1차 산출이고 `RegionFogControllerTests` 11건이 고정한다. 북극은 값만 배선해 코드 경로가 무변경 |
| 8 | EditMode 회귀 0 | **충족** | **1,207/1,207** (기준선 1,202 + 신규 5) |
| 9 | `NetworkObject` 증가 0 | **충족** | 신규 프리팹 2개 모두 `NetworkObject` 없음 |

---

## 7. 2차 — 근경 팔레트 10종 (얼음↔바다 공존)

### 7.1 구성 — 가이드 §7.5를 공존 구조로 재배치

**물길이 거의 모든 세그먼트에 들어간다** (결정 ⑰). 통짜 얼음판은 A 하나뿐이고,
그것도 "기준"으로서만 존재한다 — 나머지 9종은 전부 물을 낀다.

| # | 세그먼트 | 군 | 유형 | 가중 | 물길 | 앵커 | 내용 |
|---|---|---|---|---:|---|---|---|
| A | 설원 유빙 | 얼음 | 기본 | 0.13 | 없음 | `Ground` | 통짜 얼음. 나머지를 재는 **기준** |
| B | 압력 능선 | 얼음 | 기본 | 0.13 | 실개 ×2 | `Rock` | 얼음이 밀려 솟은 등성이 — 중경 실루엣 |
| C | 눈 덮인 바위밭 | 얼음 | 기본 | 0.13 | 좁은 ×1 | `Rock` | `Env_Rock_*` 백색 재도색 |
| D | 얼어붙은 관목 | 얼음 | 기본 | 0.13 | 좁은 ×2 | `Ground` | **신규 반입 1종** |
| E | 물길 지대 | 얼음 | 기본 | 0.13 | **넓은 ×2** | `Ground` | 물길이 주인공. **달릴지 말지가 계속 걸린다** |
| F | 갈라지는 얼음 | 전이 | 특징 | 0.085 | 좁은→열린 | `Ground` | 판이 조각으로 부서진다 — **앞이 보인다** |
| G | 빙벽 절개면 | 전이 | 특징 | 0.085 | 좁은 ×1 | `Rock` | `Env_Cliff_Face_*` 재도색 + 얼음 기둥 |
| H | 흩어진 유빙 | 바다 | 특징 | 0.08 | **열린** | `Wreck` | 조각이 작고 물이 넓다. **바다 우세의 본체** |
| I | 유적 노두 | 바다 | 이벤트 | 0.05 | 열린 | `Wreck` | 조각 위의 고대 구조물 — **최종장 복선** · `NoRepeatAdjacent` |
| J | 얼어붙은 열차 잔해 | 바다 | 이벤트 | 0.05 | 열린 | `Wreck` | 우리보다 먼저 온 열차 · `NoRepeatAdjacent` |

**군(얼음 5 / 전이 2 / 바다 3)이 §5.3의 2단 추첨 입력이다** — 구간이 먼저 정해지고,
그 안에서 세그먼트가 뽑힌다. 전이는 얼음→바다와 바다→얼음 **양쪽에서 쓰인다**.

자원 배율 2.5(타일당 **1.33개**)라 앵커는 타일당 **3~4개**면 충분하다. 가이드 §7.5의
앵커 구성(`Rock` 40 · `Wreck` 35 · `Ground` 25)을 위 배정이 만족한다.

> **앵커를 조각 위에도 놓는다.** 궤도 띠 안의 앵커는 안전하게 닿고, **조각 위 앵커는
> 물길을 넘어야 닿는다** — §5.2의 판단이 보상으로 연결되는 자리다. 다만 조각 위 앵커는
> **전 피어가 같은 자리**여야 하므로(가이드 §5.3) 스캐터 변주에 얹어선 안 된다.

### 7.1.1 바다표범 — 비적대 생물 (결정 ⑭)

가이드 §7.5는 북극에 **순록**을 배정했지만, 지형이 유빙으로 바뀌었으므로 **바다표범**으로 교체한다 —
육지 동물이 흩어진 유빙 위에 서 있으면 어색하다. 조각 위에 **1~2마리씩 누워 있다가 다가가면
물로 미끄러져 들어간다.** 신규 반입 4종의 한 자리를 쓴다(순록 자리를 대체 — 추가 비용 0).

세계관 §4.2의 *"비적대 생물을 반드시 배치"* 규정을 지키면서, **물과 얼음을 오가는 것**이
이 지역의 성질을 그대로 보여준다.

### 7.2 에셋 조달 — 재도색이 주력

| 방식 | 대상 |
|---|---|
| **백색 재도색** | `Env_Rock_S/M/L` · `Env_Rock_Cluster` · `Env_RockOutcrop` · `Env_Gravel_Pile` · `Env_Cliff_Face_A/B` · `Env_Slope_Chunk_A` · `Env_Sleeper_Old` · `Env_Signal_Rusty` · `Env_Milepost` (11종) |
| **프리미티브·ProBuilder** | 눈 언덕 · 압력 능선 · 얼음판 · 얼음 기둥 (바다 팔레트와 같은 방식) |
| **신규 반입 4종** | 얼어붙은 관목 · 유적 기둥 · 얼어붙은 객차 · **바다표범** (결정 ⑭) |

> 숲은 39종을 반입했다. **북극이 4종으로 끝나는 이유는 눈이 정체를 지우기 때문이다** —
> 이 지역에서만 통하는 절약이다.

### 7.3 완료 기준

- 이음매에서 얼음 표면 높이·재질이 끊기지 않는다 (경계면 y 0) · **물길이 이음매를 가로지르지 않는다**
- **얼음 우세 → 전이 → 바다 우세 → 전이**의 리듬이 주행 중 읽힌다 (§5.3 · 기준안 87초)
- **궤도 띠 ±8 m가 10종 전부에서 끊기지 않는다** (결정 ⑯ — 검사기 하차 대역 항목이 잡는다)
- 클리어 존 검사기가 **10종 전부 통과**(오류 0)
- 폭설(fog 0.05) 중에도 근경 실루엣으로 어느 세그먼트인지 읽힌다
- 자원 4종이 전용 메시로 스폰된다 · **조각 위 앵커가 전 피어 같은 자리에 있다**
- tris 실측 기록 · EditMode 회귀 0

---

## 8. 3차 — 물과 얼음 (게임플레이 축)

### 8.1 산출

1. `Region_Arctic`에 **`_hasWater: 1` · `_waterSurfaceY: -1.5`**
2. `PlayerSubmersion` 신설 — 이속 ×0.7 + 물 온도 −2 ℃를 체온 입력으로(단열 무효)
3. `PlayerMotor` 접지 가속 + `WorldFrameSurface` 마찰 필드 → **얼음 미끄러짐**
4. 얼음 가장자리 **맨틀** 연결 (`LadderClimbLogic.ComputeMantleMotion` 재사용)
5. 몬스터 발밑 보정 (§3.1)
6. 물속 시야·소리 — `UnderwaterView`가 이미 있다(바다 2차) · 북극 색조만
7. **얼음낚시 제한** (결정 ⑫ — §8.5)

### 8.2 이 차수의 유일한 기술 쟁점 ⚠ — 얕은 물의 0.2 m 여유

물면 −1.5 · 바닥 −2.3 → 잠김 **0.8 m**, 수영 진입은 **1.0 m**. 여유가 **0.2 m**뿐이다.
`CharacterController`의 skinWidth(0.08)·컨베이어 미세 진동·경사면 접지가 이 여유를 갉는다.

> **판정이 깜빡이면 걷기↔수영이 매 프레임 뒤집힌다** — 바다 사다리 11회차가 정확히 이 종류의
> 결함이었다(볼륨을 스치면 잡혀 다음 프레임에 꼭대기 판정). **바닥 높이를 −2.3에서 −2.2로 올려
> 여유를 0.3으로 키우는 것을 1순위 처방으로 준비**하고, 실측으로 확정한다.

### 8.3 얼음낚시 — 열되 어렵게 (결정 ⑫) ⚠

**물을 켜는 순간 낚시가 코드 0줄로 열린다** — `FishingRodController`가 `HasWater`와
`WaterSurfaceY`만 보기 때문이다. 그런데 기획서 §4.4는 북극을 *"식량 수급 거의 불가"*로,
§4.3은 대초원의 존재 이유를 *"북극행 식량 대량 비축"*으로 적어 뒀다.

> **북극에서 물고기가 쉽게 잡히면 대초원 때 비축할 이유가 사라진다** —
> 지역 하나가 다른 지역의 존재 이유를 지우는 경우다. 그래서 열되, **대가를 치르게 한다.**

| 축 | 처리 | 근거 |
|---|---|---|
| **입질 대기** | `RegionDefinition`에 배율 필드 · 북극 **×4** (2.5~12초 → 10~48초) | 한 마리에 열차 반 칸을 지나갈 시간이 든다 |
| **이중 어획** | 0.15 → **0** | 북극에서 두 마리는 없다 |
| **던질 자리** | **캐스팅 전 지형 차폐 레이캐스트 1회** | 지금은 조준선과 물면 평면의 교차만 본다 — **얼음 위에서도 던져져 찌가 얼음에 박힌다.** 차폐를 보면 *"물길·조각 사이에서만 던질 수 있다"*가 **자동으로 성립한다** |

차폐 판정은 결함 예방과 설계 의도를 **한 번에 해결한다** — 얼음낚시를 하려면 물가로 나가야 하고,
물가로 나가는 것은 §5.2의 물길을 넘는 일이다.

### 8.4 완료 기준

- 얼음 틈에 빠지면 **걷기가 유지되고 뒤로 밀리지 않는다** (§3.2)
- 얼음 벽 1.5 m를 점프로 못 넘고 **맨틀로 오른다**
- 넓은 물길(5 m)은 달려야 넘고, **달리면 미끄러져 못 멈춘다** (§5.2 ↔ §5.5)
- 흩어진 유빙 구간의 깊은 물에 빠지면 **열차가 멀어진다** — 처벌 구배가 체감된다
- 빙판에서 달리다 멈추면 **8 m 미끄러진다** · **열차 지붕에서는 안 미끄러진다** (결정 ⑬)
- **얼음 위에서는 낚싯대를 던질 수 없고**, 물가에서 던지면 대기가 눈에 띄게 길다
- 침수 중 체온이 급락하고, 물에서 나오면 회복이 시작된다
- 동상 ×0.8 × 침수 ×0.7 = **×0.56**이 곱으로 걸린다 (`IMoveSpeedModifier` 규약)
- 얼음 구간에서 **몬스터가 얼음에 묻히지 않는다**
- 호스트 1 + 클라이언트 1(MPPM)에서 물 진입·탈출이 **양쪽 화면에서 같다**

---

## 9. 4차 — 검증 마감 (36건)

### 9.1 먼저 낡은 재현 수단을 고친다 (§3.4)

Day 14 → **Day 17** · 넘패드 3 × 16회 → **F5 × 4회** · 자원 지급 **9 → 4**.

### 9.2 잔여 36건의 배치

| 구역 | 잔여 | 어느 차수 뒤에 보는가 |
|---|---:|---|
| A9 판정 · H 등록 회귀 | 1 (+H) | **1차 스모크에 포함** |
| F 강화 난방로 | 7 | 화면 무관 — 언제든 |
| W4·W5 (중간 강화 밤 · 재순환) | 2 | 화면 무관 |
| **C 부위별 동상** | **10** | **3차 뒤** — 침수가 동상 진행의 새 입력이 됐다. §3.3 구분 규칙도 여기서 판정 |
| I 북극 보스 | 7 | **2차 뒤** — 백색 평면 위에서는 거리감이 안 읽힌다 |
| J 유적 자원 | 4 | **2차 뒤** — I·J 세그먼트가 서야 "유적에서 나온다"가 성립 |
| **K 종합** | **5** | **3차 뒤** — M7 전체의 체감 판정 |

### 9.3 완료 기준

- 36건 전부 판정 · W6(방한 풀셋으로도 69초) 현행 유지 결정을 K 구역 결과로 재판단
- 발견된 결함은 수정 후 재검 · 미수정분은 잔여 정리로 이관
- EditMode 회귀 0

---

## 10. 차수 요약

| 차수 | 주제 | 코드 변경 | 볼 것 | 상태 |
|---|---|---|---|---|
| **1차** | 대자연 원경 | **계획**: fog 필드 · F5 키 → **실제**: F5 3건만 (fog 필드는 사막이 이미 세워 둠) | 지평선의 빙하 산맥 · 오로라 | **완료 (2026-08-30)** — 96 렌더러 · 3,156 tris · 셰이더 2 · EditMode 1,207 |
| **2차** | 근경 팔레트 10종 | 2단 추첨 1건 | 얼음 우세 ↔ 바다 우세 교차 · 물길 | 진행 중 |
| **3차** | 물·얼음 물리 | 4건 (§5.5) | 빠지고 · 미끄러지고 · 기어오른다 | 미착수 |
| **4차** | 검증 36건 | 결함 수정분 | M7 3차 마감 | 미착수 |

---

## 11. 리스크

| # | 리스크 | 대응 |
|---|---|---|
| 1 | ~~**사막 1차가 선행이다** (결정 ⑮)~~ **해소 (2026-08-30)** | 실제로는 사막이 **4차까지** 서 있었다. `DistantSceneryLayer`·`RegionFogController`·fog 5필드 전부 완성이고 `Region_Arctic`에 두 벌 씬 값까지 배선돼 있어, 북극 1차의 fog 일감은 **값 4개 교체**로 끝났다 (§6.3) |
| 2 | **얕은 물 0.2 m 여유**(§8.2) | 1순위 처방 준비 완료. 실측 실패 시 바닥 −2.2 |
| 3 | **폭설이 대자연을 지운다** — 확률 0.7 | **고치지 않는다.** 사막 §4.8과 같은 논리 — 세상이 지워졌다가 걷히면서 **산맥이 다시 드러나는 것**이 북극의 가장 강한 연출이다. **1차 렌더로 확인**: fog 0.05에서 능선·빙산·해면판이 전부 fog 색으로 사라지고 **하늘 겹 오로라만 남는다**(커튼 겹은 fog 를 받아 함께 지워진다 — 의도한 대로다) |
| 4 | **서리 오버레이 ↔ 동상 결빙 혼동**(§3.3) | 구분 규칙을 4차 C 구역 판정 항목으로 |
| 5 | **구간 87초가 맞는지 모른다**(§5.3) | 달려 봐야 안다 — **2차 실측 조정 항목**. 값은 상수 하나라 바꾸기 싸다 |
| 6 | **2단 추첨이 다섯 지역 공유 경로를 건드린다**(§5.6-5) | 구간 크기 0이면 **현행 독립 추첨 그대로**로 떨어지게 만든다 — 다른 네 지역은 이 값을 비워 둔다(팔레트 폴백과 같은 규약) |
| 7 | **북극 보스가 사막 보스와 구분되지 않는다** | 실측: 두 프리팹 diff가 `GlobalObjectIdHash` 한 줄뿐이고 시그니처 패턴도 둘 다 2(투사체)다. **결정 ⑪로 계획 밖** — 검증 I 7건은 "구분되지 않는다"를 결함으로 **기록만** 하고 넘긴다 |
| 8 | 검증 중 결함이 M7 3차 **설계**로 소급된다 | 이 계획은 재설계를 소유하지 않는다 — 별도 차수로 뗀다 |

---

## 12. 이 계획이 진행되면 갱신할 문서

| 문서 | 갱신 내용 |
|---|---|
| [M7 3차 플레이 검증 항목](../M7/M7-3차-플레이-검증-항목.md) | 재현 수단 3건 정정 · 잔여 36건 판정 |
| [M6-M8 잔여 작업 정리 §2.4](../M6-M8-잔여-작업-정리.md) | 검증 잔여 57 → 21 |
| [region/region-timeline §5](../../specs/region/region-timeline.md) | `RegionDefinition` 표에 북극 열 · fog 3필드 · `HasWater` 두 번째 사용처 |
| [레벨 디자인 가이드 §7.5](../../design/Train-Survival-레벨디자인-가이드.md) | **북극이 육지 설원이 아니라 얼음↔바다 교차임을 반영** (as-built) |
| [사막 지역 구현 계획 §4](사막-지역-구현-계획.md) | `DistantSceneryLayer`·fog 필드의 **두 번째 소비자**로 북극을 명시 |
| [player/network-movement](../../specs/player/network-movement.md) | 접지 가속(미끄러짐) · `PlayerSubmersion` |
| [아트·렌더링 예산 §6](../../design/Train-Survival-아트-렌더링-예산.md) | 북극 타일·원경 실측 |
| [qa-debug-keys](../../guide/qa-debug-keys.md) | F5(지역 점프) + 키 지도 |
| [plans/README.md](../README.md) | 상태 갱신 |

---

## 13. 결정 기록

| 일자 | 결정 | 근거 |
|---|---|---|
| 2026-08-29 | 화면 + 검증 통합 1문서 | 검증 I·K가 화면을 기다린다 — 두 번 판정하지 않기 위해 |
| 2026-08-29 | 축을 원경으로 (*"대자연"*) | 사용자 지시. fog 0.0062가 300 m에서 3 %라 원경이 **존재해도 안 보이는 상태**였다 |
| 2026-08-29 | 얼음 + 바다 교차 (결정 ①~④) | 사용자 지시. 바다 4차의 물면 주행·수면 도약이 이미 서 있어 **위협이 물에서 오는 그림**을 재사용할 수 있다 |
| 2026-08-29 | 오로라 품질 우선 (결정 ⑦) | 사용자 지시 — *"아름답게 표현할 수 있는 방식으로, 중요하다"* |
| 2026-08-30 | **교차는 폭이 아니라 조각 크기·밀도** (결정 ⑯·⑰) | 사용자 지시 — *"얼음과 바다가 공존하며, 얼음 비율이 높을 경우 얼음 땅이 대부분 사이사이 바다, 바다 비율이 높을 경우 유빙 느낌으로 흩어져"*. 초안의 "±8 m 한 줄 다리"는 오독이었다. 조각 구조가 더 낫다 — 몬스터 주행 대역 문제가 풀리고(조각을 밟고 온다) 물길 넘기가 위험 보상이 된다 |
| 2026-08-30 | 얼음낚시 열되 어렵게 (결정 ⑫) | 물을 켜면 낚시가 코드 0줄로 열리는데, 쉬우면 **대초원의 존재 이유**(기획서 §4.3)가 사라진다 |
| 2026-08-30 | 지붕 결빙은 구조만 (결정 ⑬) | 사용자 지시 — *"일단 기능 구현하고 차후 반영 여부 결정하자"* |
| 2026-08-30 | 동적 균열 없음 | 지형이 변하면 복제가 필요해 **불변 규약(NetworkObject 0개)** 위반 |
| 2026-08-30 | 사막 먼저 (결정 ⑮) | 원경 인프라 2건의 소유자를 사막으로 확정 |
| 2026-08-30 | **⑱ 중경을 시차 층으로 — 계획 §4.1을 개정한다** | §4.1 표는 중경(60~300 m) 소유자를 "타일 지면판"(6 m/s)으로 적었는데, 그 표는 **사막 as-built 이전**의 것이다. 사막 결정 ⑧이 중경 시차 대역(0.35)을 4층 공통 규격에 넣었고(가이드 §7.1), 계획 §4가 *"층·거리·시차계수 규격은 사막이 소유한다"* 고 적어 두었다 — **공통 규격을 따른다.** 대가는 렌더러 25개·750 tris |
| 2026-08-30 | **⑲ 오로라 커튼은 알파가 아니라 가산 합성이다** | 완료 기준이 *"커튼이 산맥을 지우지 않는다"* 라 계획 §4.4는 **렌더 큐 고정**을 처방했다. 가산(One One)이면 순서와 무관하게 뒤가 남으므로 **문제 자체가 사라진다** — 렌더 큐 주의가 필요 없어졌다. A/B 실측: 커튼을 켜서 어두워진 화소 **46 / 921,600** |
| 2026-08-30 | **⑳ 눈보라 입자를 1차에 넣는다** | 완료 기준 *"오로라가 폭설 중에도 하늘에 남는다"* 를 판정하려면 폭설이 **화면에** 있어야 하는데 북극 날씨 연출은 fog뿐이었다. 사막 4차 `WeatherParticleView`를 **한 줄도 안 고치고** 재사용(결정 ⑲가 담당 날씨 슬롯을 그 목적으로 만들어 뒀다). 코드 0 · 신규 텍스처 0 |
| 2026-08-30 | **㉑ 능선 실루엣 3종** | 한 메시를 여섯 번 늘어놓으니 **같은 산이 반복되는 것**이 800 m 에서도 읽혔다. 사막이 "능선 3종"을 쓴 이유가 여기서 확인됐다. 대가는 +306 tris(총 예산의 12 %) |
| 2026-08-30 | **㉒ 북극 밤 fog = `#2A4257`** | 계획 §4.2는 낮 값(`#DCE8F0` 0.0017) 하나만 정했는데 필드는 낮·밤 2벌이다. 사막(`#2B3A63` 남색)보다 **청록 쪽**으로 잡았다 — 사막 밤이 "달빛 아래 모래"라면 북극 밤은 "오로라가 비친 얼음"이고, 하늘 겹 오로라가 초록이라 fog 가 순남색이면 두 색이 싸운다. 밀도는 낮과 같은 0.0017(밤에도 대자연을 유지 — 사막 결정 ⑦과 같은 논리) |

---

## 14. 착수 시 첫 작업

**사막 1차의 진행 상태를 먼저 확인한다**(리스크 1). `DistantSceneryLayer`와 `RegionDefinition`
fog 필드가 이미 있으면 북극 1차는 **에셋 배치만**으로 끝나고, 없으면 이 계획이 그 둘을 만든다.

그다음이 `Region_Arctic.asset`이다 — 이 지역은 `_segmentPalette`·`_skyboxMaterial`·`_hasWater`가
**전부 비어 있어**, 어느 차수든 첫 손은 YAML 키를 채우는 일이다.

---

## 15. 관련 문서

| 문서 | 관계 |
|---|---|
| [M7 3차 구현 계획](../M7/M7-3차-구현-계획.md) | **북극 시스템의 소유자** — 이 계획은 검증만 한다 |
| [사막 지역 구현 계획](사막-지역-구현-계획.md) | **원경 3층 구조의 원본** — `DistantSceneryLayer`·fog 필드를 공유한다 |
| [바다 지역 구현 계획](바다-지역-구현-계획.md) | **물의 원본** — `HasWater`·수영·물면 주행·수면 도약을 북극이 재사용한다 |
| [레벨 디자인 가이드](../../design/Train-Survival-레벨디자인-가이드.md) | 규격의 단일 진실 원천 (§7.5가 북극 콘텐츠) |
| [기획서 §8.3](../../design/Train-Survival-기획서.md) | 최종장 — 세그먼트 I(유적 노두)가 놓는 복선의 목적지 |
