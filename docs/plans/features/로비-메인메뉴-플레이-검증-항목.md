# 로비 · 메인 메뉴 플레이 검증 항목 — 1~6차 통합

작성일: 2026-08-20 · 대상: `Main.unity` (main = `2d0d17c`) · 상태: **검증 대기**
(구현: [로비·메인 메뉴 구현 계획](로비-메인메뉴-구현-계획.md) §7의 **1~6차 전부**.
EditMode **700/700 통과** 상태에서 시작 — 1차 이전 659 + 신규 41.
**사운드는 이번 범위가 아니다** — §11 미결 8)

> **무엇을 보는가**: 이 화면의 목표는 `Lobby 시안.png`를 **동작하는 화면으로 만드는 것**이었다.
> 그러므로 검증은 두 축이다 — **그림이 시안으로 읽히는가**(A·B·I)와 **손에 걸리는 데가 없는가**(C~G).
> 마지막 H는 이 화면 고유의 질문이다: **30초 두었을 때 그림 한 장으로 보이지 않는가.**
>
> **검증 구성**: 대부분 **단독 실행**으로 충분하다. G 구역(세션)만 **호스트 1 + 클라이언트 1**
> (MPPM 가상 플레이어)이 필요하다.

---

## 0. 검증자가 먼저 읽을 것 — "결함 아님" 다섯 가지

아래는 전부 **의도된 상태**다. 결함으로 올리지 않는다.

1. **시안과 완전히 같지 않다 — 같을 수 없다** (§13 ㉘).
   실측 결과 **시안에 그려진 기차는 자기 선로 위에 있지 않다.** 후미 바닥이 레일의 소실점
   (화면 0.678)보다 위로 올라가는데, 지면 물체가 수평선보다 높은 셈이라 물리적으로 불가능하다.
   **시안 완전 재현과 선로 정합은 동시에 성립하지 않아 선로 쪽을 택했다.**
   → 그 결과 **기관차가 시안보다 약간 더 측면으로 보인다.** 이것이 이번 차수의 의도적 차이다.

2. **버전이 `v0.1.0`이다** (§13 ㊱). 시안의 `v0.2.1`은 목업 값이고, 라벨은
   `Application.version`을 읽는다. 숫자를 올리려면 Player Settings를 바꾼다.

3. **소리가 하나도 없다** (§11 미결 8). 프로젝트에 오디오 인프라가 아직 없다 —
   [오디오 가이드](../../design/Train-Survival-오디오-가이드.md) §21.3의 1차가 선행이다.

4. **오로라가 흐르지 않는다** (§11 미결 4). 배경 레이어 분리 재생성이 필요해 범위 밖이다.
   오로라는 배경 그림에 그려진 정지 이미지다.

5. **하위 패널(여정 시작·업적·설정·공고)이 밋밋하다** (§13 ㉖). 회색 판에 버튼만 있다.
   정식 스타일링은 [UI 구현 계획](UI-구현-계획.md)의 몫이라 자리와 흐름만 잡았다.

6. **에디터에서 키보드가 처음엔 안 듣는다** — 게임 뷰에 **포커스가 없으면** 키 입력이
   에디터 창으로 간다. 클릭 한 번이면 풀린다. 1회차 검증에서 "한 번 클릭해야 작동한다"로
   올라왔으나 **결함이 아니다.** 7차에 플레이 모드에서 실측했다 —
   **프레임 2에서 이미 `Plate_Achievements`가 선택되어 있었고**(`EventSystem.currentSelectedGameObject`),
   입력 모듈도 활성 상태였다. 빌드에는 이 단계가 없다. 게임 뷰 툴바의 **Play Focused**를
   쓰면 에디터에서도 바로 듣는다.

---

## 0.1 사전 준비

- [ ] **씬 변경 1건** — `Main.unity` 전면 재구성 (1~6차 누적). 다른 씬은 건드리지 않았다
- [ ] **`MainMenuController.cs` 삭제됨** — 좌상단 IMGUI 임시 메뉴는 더 이상 나오지 않는다
- [ ] **실행 경로 두 가지를 모두 본다**
  - **Boot부터** — 세션 서비스가 등록되어 "게임 시작"이 **열린다** (G 구역은 반드시 이쪽)
  - **Main 단독** — 세션 서비스가 없어 "게임 시작"이 **잠긴다**(의도). C4가 이 경우다
- [ ] **게임 뷰에 포커스를 준다** — 에디터가 비포커스면 플레이 모드가 프레임을 돌리지 않아
      **드리프트·명멸·페이드가 멈춘 것처럼 보인다**(H 구역이 통째로 무의미해진다)

**기준 수치** (전부 as-built — 계획 §13.1~13.7)

| 항목 | 값 | 어디서 확인 |
|---|---|---|
| 카메라 | pos (0, 1, −10) · rot **0** · FOV **50** | `Main Camera` |
| 기차 | pos **(−1.82, −2.10, 7.03)** · yaw **136.81°** | `Train_Locomotive_Menu` (§13.5) |
| 궤도 중심선 기울기 | **0.3890** = 기차 바닥축 기울기 | B1의 근거 |
| 배너 | 화면 높이 **×1.5448**, 종횡비 940:1672 고정 | `MenuPlateLayout` |
| 명판 높이 | 위에서부터 **113 → 108 → 109 → 105 px**(원본 기준) | 균등 아님이 정상 |
| 호버 | 알파 0→1 **0.12초** · 크기 **1.00→1.02** | `MenuPlateButton` |
| 카메라 드리프트 | 위치 **±0.03 m** · 회전 **±0.4°** · 주기 12~21초 | `MenuCameraDrift` |
| 창문 명멸 | 밝기 **×0.86~1.14** · 주기 **9초** | `MenuWindowFlicker` |
| 입장 페이드 | 검정 → 화면, **1.2초** | `MenuScreenFade` |
| 예산 | tris **291,049** / 50만 · 텍스처 **약 33 MB** / 80 MB | §10 완료 기준 |

---

## A. 첫인상 — 시안 대조 (§8.2-1)

`Lobby 시안.png`를 옆에 띄우고 본다. **§0-1의 차이는 제외하고** 판정한다.

- [0] **A1** 배너(표지판)의 **크기와 자리**가 시안과 같다 — 좌측 가장자리에 붙고 위아래가 잘린다
- [0] **A2** 타이틀 각인 `TRAIN SURVIVAL`이 시안과 **같은 높이·같은 크기**다
- [0] **A3** 명판 4장의 **세로 간격이 균등하지 않다** (아래로 갈수록 좁다) — 원근이 살아 있다
- [0] **A4** 배너에 **회색 테두리나 잘린 자국이 없다** — 잎사귀 가장자리·격자 기둥 사이 틈 확대 확인
- [0] **A5** 공고문 종이가 우측에 있고 **글자가 구워진 흔적(흐릿한 얼룩)이 없다**
- [0] **A6** 전체 톤이 시안처럼 어둡다 — 회색으로 씻겨 보이지 않는다

## B. 기차 · 선로 정합 (§8.2-2·3)

- [X, 방향은 똑같지만 뒷바퀴가 들려있다.] **B1** **기차가 선로와 나란하다** — 기관차부터 후미까지 바퀴가 레일을 따라간다.
      *(4차 이후 재정합의 핵심 항목. 축이 레일을 가로지르면 즉시 보고)*
- [X] **B2** 카우캐처·바퀴가 **배경 레일에 얹혀** 보인다 — 뜨거나 파묻히지 않는다
- [0] **B3** 후미 객차가 **배너 뒤로 자연스럽게 사라진다** — 배너 경계에서 잘린 티가 나지 않는다
- [0] **B4** 배경 램프(좌측 플랫폼)와 **3D 기차의 하이라이트 방향이 같은 쪽**이다
- [0] **B5** 기차가 배경보다 **밝거나 채도가 튀지 않는다** — 붙여 놓은 것처럼 보이면 결함

## C. 명판 상호작용 (§8.2-4)

- [0] **C1** 명판에 마우스를 올리면 **황금 명판이 떠오르고** 화살표가 그 줄로 옮겨 온다
- [0] **C2** **한 번에 한 장만** 빛난다 — 마우스와 키보드가 각각 다른 줄을 켜지 않는다
- [0] **C3** **색 없이도** 어느 줄이 선택됐는지 안다 — 화살표 위치와 크기 변화로 읽힌다
      *(흑백 스크린샷으로 확인하면 확실하다)*
- [0] **C4** **Main 단독 실행**에서 "게임 시작"이 **잠겨 있고**, 포커스가 "업적"에 있다
- [0] **C5** 잠긴 "게임 시작"을 눌러도 아무 일이 없고, **위아래 이동이 그 줄을 건너뛴다**
- [0] **C6** 각 명판이 제 패널을 연다 — 업적/설정은 스텁, 종료는 **에디터 플레이가 멈춘다**

* 명판이 내려갈수록 이질감이 있다.
기존 노말과 호버가 전체적으로 조금씩 맞지 않는 것 같다.
따라서 각 명판마다 텍스처를 따로 빼서 배치하면 좋을 것 같다.
## D. 문구 (§8.2-5)

> 원래 기준은 "`Ver1.png`와 어긋나지 않는다"였으나, 3차 실측에서 **`Ver1`/`Ver2`가 픽셀 정렬이
> 아님**이 밝혀졌다(§13 ⑯). 그래서 기준을 **시안 대조**로 바꾼다.

- [0] **D1** `게임 시작`·`업적`·`설정`·`종료`가 명판 **가운데**에 있고 판을 넘지 않는다
- [0] **D2** 글자 크기·굵기가 시안과 비슷하다 — 가늘거나 뭉개져 보이지 않는다
- [0] **D3** **황금 명판 위에서도 글자가 읽힌다** — 검은 외곽선이 대비를 만든다
- [0] **D4** 공고문의 `운행 공고`와 요약 3줄이 **장식(구분선·마무리)과 겹치지 않는다**
- [0] **D5** 좌하단 버전이 `v0.1.0`, 우하단이 `✛ 선택` · `E 확인`이다

## E. 키보드 · 게임패드 (§8.2-6)

**마우스에서 손을 뗀 채로** 진행한다.
* 조건부가 있다. 한번 UI클 릭하고 난 이후 작동한다.
- [O] **E1** ↑↓로 명판 4장을 오갈 수 있다
- [O] **E2** 맨 아래에서 ↓를 누르면 **맨 위로 돌아온다** (순환)
- [O] **E3** Enter/Space로 선택한 항목이 열린다
- [O] **E4** 패널이 열리면 **포커스가 패널 안으로** 들어온다 — 뒤쪽 명판이 입력을 훔치지 않는다
- [X] **E5** **Esc**로 패널이 닫히고 **포커스가 원래 명판으로** 돌아온다
- [O] **E6** **게임패드**로 E1~E5가 모두 된다 (D-패드/스틱 = 이동, A = 확인, B = 취소)
      *(배선은 표준 입력 모듈 경유라 되어야 하지만 실기 확인은 이번이 처음이다)*

## F. 종횡비 (§8.2-7)

Game 뷰 해상도를 바꿔 가며 본다. **1080p / 1440p / 16:10(1920×1200) / 21:9(2560×1080)**.

- [0] **F1** 배경에 **여백(검은 띠)이 전혀 없다** — 네 해상도 모두
- [0] **F2** 배경이 **찌그러지지 않는다** — 종횡비가 유지된 채 잘린다
- [0] **F3** 배너가 **늘어나지 않는다** — 잘리는 것은 왼쪽과 위아래뿐
- [0] **F4** 명판 4장이 **화면 안에 전부** 있다 (왼쪽으로 잘려 나가지 않는다)
- [0] **F5** 공고문이 **오른쪽으로 잘리지 않고**, 배너와 겹치지 않는다
- [0] **F6** 버전·컨트롤 가이드가 화면 모서리에 붙어 있다

## G. 세션 흐름 (§8.2-8·9) — **Boot부터 실행**

- [0] **G1** Boot를 거치면 "게임 시작"이 **열린다**(잠금 해제)
- [0] **G2** 게임 시작 → **새 여정**이 호스트 세션을 열고 인게임 씬(`Game_ArtTest`)을 로드한다
- [0] **G3** 개발용 **씬 토글**이 `Game ↔ Game_ArtTest`를 바꾸고, 바꾼 쪽으로 들어간다
- [0] **G4** **직결 모드** — 주소 칸이 `127.0.0.1`로 채워져 있고, 호스트를 띄운 뒤
      다른 인스턴스에서 **친구 참가(주소 접속)**로 들어가진다
- [차후 확인한다.] **G5** **Steam 모드** — 호스트 시작이 친구 전용 로비 생성을 겸하고,
      **친구 초대가 Steam 오버레이를 실제로 연다** *(이전에는 안내 문구뿐이었다)*
- [ ] **G6** 세션이 시작된 뒤 패널 버튼이 **잠긴다** — 두 번 눌러 두 세션이 열리지 않는다
- [0] **G7** 콘솔에 에러가 없다

## H. 연출 — 30초 (§8.2-10)

**아무것도 만지지 않고 30초 본다.** 게임 뷰에 포커스가 있어야 한다(§0.1).

- [0] **H1** 30초 뒤에도 화면이 **그림 한 장으로 읽히지 않는다** — 이 구역의 총평이다
- [0] **H2** **카메라가 아주 조금 움직이고**, 그때 **기차만 배경에 대해 미끄러진다**
      (배너·배경은 함께 움직이지 않는다 — 그것이 시차다)
- [0] **H3** 드리프트가 **떨림이 아니라 흐름**으로 보인다 — 프레임마다 튀지 않는다
- [0] **H4** 굴뚝에서 **옅은 증기**가 오르고, **하늘을 덮지 않는다**(오로라가 가려지면 결함)
- [0] **H5** 하부 실린더에서 **주기적으로** 배기가 터진다 (약 1.3초 간격)
- [0] **H6** 객차 창문 불빛이 **느리게 흔들리고 꺼지지 않는다**
- [0] **H7** 헤드램프가 **화면에서 가장 밝은 점**이고 Bloom이 번진다
- [0] **H8** 진입 시 **검은 화면에서 1.2초에 걸쳐 밝아진다** (Boot → Main 전환에서 확인)

## I. 공고판 · 패치 노트 (6차)

- [0] **I1** 공고문을 **클릭하면** 패치 노트 패널이 열린다
- [0] **I2** 패널의 버전이 좌하단 라벨과 **같은 숫자**다
- [닫기는 0, Esc는 X] **I3** 닫기 버튼과 **Esc** 양쪽으로 닫힌다
- [뭍어있는 것 같다.] **I4** 공고문 종이의 **가장자리에 배경이 묻어 있지 않다**

## J. 회귀 · 위생

- [0] **J1** 인게임(`Game_ArtTest`) 진입 후 **HUD·플레이가 이전과 동일**하다
      — 이번 작업은 인게임을 건드리지 않았다
- [0] **J2** 인게임에서 나와 다시 로비로 돌아오는 경로가 있으면 그때도 화면이 정상이다
- [0] **J3** 로비에 30초 있는 동안 **프레임이 안정**적이다 (파티클·후처리 부담 확인)
- [0] **J4** 콘솔에 **에러·경고가 없다**

## N. 공고대 재검증 (7차, 2026-08-21)

> 1회차 검증에서 **I4가 실패**로 올라와, 공고문을 기둥·받침이 있는 **공고대**로 교체했다
> (계획 §13.8). I 구역은 이 구역으로 다시 본다 — I1·I2·I3은 그대로 유효하다.

- [ ] **N1** 공고대가 오른쪽에 **서 있는 물건으로** 보인다 — 종이가 허공에 뜨지 않는다
- [ ] **N2** **받침이 화면 아래로 잘려 있다** — 받침이 다 보이면 결함이다(계획 §13 ㊴)
- [ ] **N3** 기둥 머리(윗단)가 **잘리지 않는다** — 위쪽 여백은 화면 높이의 0.5 %뿐이라 빡빡하다
- [ ] **N4** 종이 가장자리에 **배경이 묻어 있지 않다** — **I4의 처방 확인**
- [ ] **N5** 요약 3줄이 장식 두 줄 **사이 가운데**에 있고, 원화 글줄과 **같은 각도로** 기울어 있다
- [ ] **N6** 제목 `운행 공고`는 그림이다 — 요약과 **글꼴이 달라 보이는 것이 정상**이다
- [ ] **N7** **1080p / 1440p / 16:10 / 21:9**에서 종이가 잘리지 않고 명판과 겹치지 않는다
      *(4:3에서 배너 스프라이트와는 겹치지만 명판과는 259 px 떨어져 있다 — 결함이 아니다)*
- [ ] **N8** 공고대를 클릭하면 패치 노트가 열린다 — **I1 회귀 확인**
- [ ] **N9** 공고대가 배경보다 **밝거나 날카롭지 않다** — 붙여 놓은 것처럼 보이면 결함

## O. 검증 후속 수정 재확인 (7차, 2026-08-21)

- [ ] **O1** **Esc**로 업적·설정·공고 패널이 모두 닫히고, 포커스가 **원래 명판으로** 돌아온다
      *(E5·I3의 처방. 게임패드 B도 같은 경로다)*
- [ ] **O2** 아무것도 강조되지 않은 상태에서 **명판 4장의 색·질감이 서로 같다**
      — "게임 시작"만 금색으로 남아 있으면 결함이다
- [ ] **O3** 명판 4장을 차례로 강조해 본다 — **줄마다 테두리가 이중으로 보이지 않는다**
      (아래 명판이 위 그림 밖으로 비어져 나오면 결함)
- [ ] **O4** 강조된 금색 명판의 **색이 네 줄 모두 비슷하다** — 아래로 갈수록 탁해지면 결함
- [ ] **O5** 마우스를 빠르게 위아래로 훑어도 **두 장이 동시에 금색이 되지 않는다** (C2 회귀)

---

## 판정

- **B1 실패** → 선로 정합 회귀. 4차 이후 재정합(§13.5)이 무효화된 것이므로 **즉시 중단·보고**
- **C·E 실패** → 상호작용. 순수 로직(`MenuNavigation`·`MenuPlateLayout`)은 EditMode 전수
  통과 상태이므로 **배선·씬 쪽을 먼저** 본다
- **F 실패** → 배치 계산. `MenuPlateLayout`·`BackgroundCoverMath`·`NoticeBoardView`의
  상수 세 벌이 유일한 출처다
- **G 실패** → 4차 세션 이관. `MenuSessionActions`가 옛 `MainMenuController`와 같은 순서로
  부르는지 확인한다
- **H1이 "그림 한 장"이면** → 드리프트 진폭·주기를 재검토한다. 계획 §6.4가
  "이 화면의 성패를 가른다"고 본 지점이다
- **§0의 다섯 항목은 결함으로 올리지 않는다**

---

## 알려진 이슈 · 이월

| 항목 | 상태 |
|---|---|
| **사운드 4종** | §11 미결 8 — 오디오 인프라(가이드 §21.3 1차)가 선행 |
| **오로라 흐름** | §11 미결 4 — 배경 레이어 분리 재생성 필요 |
| **디스플레이 서체** | §11 미결 7 — 차후 별도 조사. 현재 UI 폰트는 Noto Sans KR(OFL) |
| **하위 패널 스타일** | §13 ㉖ — 색이 씬 데이터라 `UiPalette`를 따라가지 않는다. 정식 스타일링은 UI 구현 계획 |
| **OFL 전문 미동봉** | `Art/Fonts/NOTICE.md` — 배포 전 `OFL.txt` 동봉 필요 |
| **`Game.unity` 무영향** | 이번 작업은 `Main.unity`만 건드렸다 |

---

## 검증 결과 기록란

> 회차를 진행하며 여기에 적는다. 통과는 위 체크박스를 `[x]`로 바꾸고,
> 실패·발견은 아래에 **구역-번호**로 남긴다.

### 1회차 (2026-08-21) — 위 체크박스는 사용자가 직접 기입했다

**통과**: A 전부 · B3~B5 · C1~C6 · D 전부 · E1~E4·E6 · F 전부 · G1~G4·G7 · H 전부 · I1·I2 · J 전부
**미확인**: G5(Steam 모드) · G6(이중 세션 방지)

| # | 구역 | 증상 | 원인 | 처방 | 상태 |
|---|---|---|---|---|---|
| 1 | **B1** | 방향은 레일과 같으나 **뒷바퀴가 들려 있다** | 미조사. 4차 재정합(§13.5)은 **바닥축의 화면 기울기**만 맞췄고, 차량 길이 방향의 **피치**는 검증 항목에 없었다 | 조사 후 처방 | **미결** |
| 2 | **B2** | 카우캐처·바퀴가 레일에 **얹혀 보이지 않는다** | 1번과 같은 뿌리로 보인다 | 1번과 함께 본다 | **미결** |
| 3 | **E5 · I3** | **Esc로 패널이 닫히지 않는다** (닫기 버튼은 된다) | **규명 완료.** 취소는 계층을 타고 올라오지 않는다 — 입력 모듈이 `ExecuteHierarchy`가 아니라 **`Execute`**로 보낸다(`InputSystemUIInputModule.cs:902`). 패널 안에서 선택된 것은 언제나 버튼이라 패널 루트의 `ICancelHandler`에 취소가 영영 닿지 않았다. 4차 XML 주석의 전제가 틀렸다 | `MenuCancelRelay` 신설 — `MenuPanel.Open`이 자기 밑 모든 `Selectable`에 런타임으로 붙인다. 플레이 모드에 Escape를 주입해 패널 3종이 닫히고 포커스가 **원래 명판으로** 돌아옴을 실측 | **해결 (2026-08-21)** |
| 4 | **E 전체** | 키보드가 **UI를 한 번 클릭한 뒤에야** 듣는다 | **결함이 아니다 — 게임 뷰 포커스다.** 플레이 모드 실측에서 **프레임 2에 이미 `Plate_Achievements`가 선택**돼 있었고 입력 모듈도 활성이었다 | §0-6에 기록. 에디터에서는 게임 뷰의 **Play Focused**를 쓰면 처음부터 듣는다 | **해결 (2026-08-21)** |
| 5 | **C 전체** | 명판이 **아래로 갈수록 이질감**이 있다 — 노말·호버가 조금씩 어긋난다 | **규명 완료.** 3차가 명판 한 장을 크롭해 네 줄에 늘여 썼다. 배너 원화의 네 명판은 **높이도 리벳 자리도 서로 다르다**(117·113·113·111 px). 자리는 이미 `MenuBannerView`가 정확값으로 덮고 있었으니 **모양 문제**였다 | 사용자 제안대로 **슬롯마다 크롭** — `T_Menu_Plate{0..3}_Normal/Hover` 8장. 원화에 없는 상태(0번 어둠 · 1~3번 금색)는 합성했고, 배너의 0번 명판도 어둡게 칠해 배너 자체를 평상 상태로 만들었다(§13.9 ③) | **해결 (2026-08-21)** |
| 6 | **I4** | 종이 가장자리에 **배경이 묻어 있다** | 6차 종이는 시안에서 **키잉**해 뽑은 것이라 경계에 잔상이 남았다 | **7차에 해소** — 알파 채널이 있는 원화로 교체(계획 §13.8). §N4로 재확인 | **해결 (2026-08-21)** |
| 7 | **G5 · G6** | 미확인 | — | 차후 확인 | **보류** |

* 현재 ESC가 먹히지 않는 것은 에디터 탓일 수도 있을 것 같다.